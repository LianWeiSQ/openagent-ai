# Goal State

- original_user_request: 把 OpenAgent 做成对标 Codex / Zcode / Claude Code Desktop 的 Rust-first 桌面 Agentic Coding Workspace。
- objective_locked: Rust Desktop Agentic Coding Workspace；优先 Desktop/App Bridge/Agent Runtime 可见闭环，Python 只作历史参考，不作为产品主线。
- non_goals:
  - 不继续补 Python runtime。
  - 不推 GitHub，除非 William 明确要求。
  - 不把长期目标缩小成 CLI-only 或 MVP-only。
- success_criteria:
  - Rust CLI 能通过 Sub2API `gpt-5.4-mini` 正常 run，不需要 Python。
  - Rust doctor 真实检查 provider `/v1/models`，不再用假健康变量。
  - App Bridge 能被 CLI/TUI/Desktop 共用。
  - Desktop app 能完成“选择项目 -> 新建会话 -> 流式 run -> tool approval -> diff -> checkpoint -> rollback”的产品闭环。
  - session/message/tool/diff/checkpoint 可持久化并恢复。
- current_slice: HTTP runtime choke point split 完成；等待下一条用户可见大闭环。
- slice_boundary:
  - `runtime/http/src/http_runtime.rs`
  - `runtime/http/tests/http_runtime.rs`
  - `runtime/app-server-client/src/app_bridge_client.rs`
  - `runtime/tui/src/commands.rs`
  - `runtime/tui/src/server.rs`
  - `runtime/tui/src/tests.rs`
  - `tests/golden/rust_rewrite/app_bridge_tui.json`
  - `cli/src/mcp.rs`
  - `cli/src/help.rs`
  - `cli/src/util.rs`
  - `cli/tests/cli_commands.rs`
  - `desktop/`
  - `src/session/src/session_store.rs`
  - `src/session/tests/session_trace.rs`
- allowed_actions: 本地 Rust/TS 代码修改、目标测试、App Bridge Sub2API smoke、Desktop browser smoke；不提交、不推送。
- interrupt_policy: William 说 stop/status/pause 时立即交还控制权。
- execution_mode: 大闭环、少测试、少文档、先拆核心瓶颈；receipt 只写 changed / verified / next。
- last_receipts:
  - 2026-07-03: changed: `runtime/http/src/http_runtime.rs` 拆出 `app_bridge_routes.rs`、`mcp_runtime.rs`、`turn_runtime.rs`，保留行为不变并开放必要 `pub(super)` 边界。verified: `cargo fmt --all -- --check`、`cargo check -p openagent-http-runtime`、MCP lifecycle 关键测试、trust/checkpoint 关键测试均通过。next: 做用户可见闭环，优先 Desktop MCP panel 或 approval dock。
  - 2026-07-03: CLI MCP local test and App Bridge lifecycle guard slice 完成。本轮收口上一段残留的 CLI standalone MCP 语义：`cli/src/util.rs` 的 `mcp_config_path` 现在同时支持 `--mcp-config` 与 `--config`，与 `run --mcp-config` 主路径对齐；`cli/src/mcp.rs` 新增 `mcp test <server> --mcp-config <file>` 本地一次性连通测试，会读取本地 MCP config，执行一次 `discover_mcp_server_tools` / `tools/list`，返回脱敏 snapshot、selected_transport、tool_count、tools，不保活本地 stdio 进程；`mcp start|stop|restart|enable|disable` 如果带本地 `--mcp-config`/`--config`/`--workspace`/`--dir`，会在发网络请求前失败并明确提示这些命令使用 App Bridge lifecycle registry，需要先启动 Rust App Bridge 后用 `--server-url`/`--server-token`；显式 remote 模式同时传本地 config/workspace flag 也会失败，避免客户端悄悄忽略本地配置；remote positional parser 增加本地 config/workspace flags，防止配置路径被误当成 server name；`cli/src/help.rs` 把 MCP help 分成 local one-shot test 与 App Bridge lifecycle 两条路径。新增测试：`binary_mcp_test_uses_local_mcp_config_alias_once` 用 `--mcp-config` 指向临时 disabled stdio MCP server，验证本地一次性 test 成功发现 1 个 stdio tool；`binary_mcp_lifecycle_rejects_local_config_without_app_bridge` 验证 `mcp start ... --mcp-config` 直接给出 App Bridge lifecycle registry 引导且不出现 connection refused；远端 lifecycle 回归 `binary_mcp_remote_lifecycle_controls_app_bridge` 仍通过。验证：`cargo test -p openagent-cli binary_mcp_test_uses_local_mcp_config_alias_once --test cli_commands -- --nocapture` 通过；`cargo test -p openagent-cli binary_mcp_lifecycle_rejects_local_config_without_app_bridge --test cli_commands -- --nocapture` 通过；`cargo test -p openagent-cli binary_mcp_remote_lifecycle_controls_app_bridge --test cli_commands -- --nocapture` 通过；`cargo test -p openagent-cli binary_help_smoke_covers_legacy_command_surface --test cli_commands -- --nocapture` 通过；`cargo fmt --all -- --check` 通过；`cargo check -p openagent-cli` 通过；`git diff --check` 与显式 trailing whitespace/tab 检查通过；无 openagent runtime / stdio MCP / Vite 残留进程。残留：CLI 仍没有自己持久化 local lifecycle registry，这是有意保持 App Bridge-first；下一步若要再收口 MCP，应做 App Bridge lifecycle 跨重启恢复/健康缓存，或 TUI MCP picker/remote control route。
  - 2026-07-03: TUI remote MCP lifecycle controls via App Bridge slice 完成。本轮把上一段 CLI 远端 MCP lifecycle 控制继续推进到 Rust TUI 接入层：`runtime/tui/src/commands.rs` 的 `/help` 新增 `/mcp [list|show|doctor|test|start|stop|restart|enable|disable]`；`runtime/tui/src/server.rs` 在 `AppBridgeTerminalHandler` 中新增 `/mcp` slash command 分支，直接复用 `RemoteRuntimeClient::mcp_status`、`mcp_server_test`、`mcp_server_lifecycle`、`mcp_server_update`，支持 `/mcp`/`/mcp list`/`/mcp doctor` 查看 App Bridge MCP 状态，`/mcp show local-tools` 展示单 server 和 tools，`/mcp start|stop|restart|enable|disable|test local-tools` 控制远端 lifecycle，并把 enabled/status/transport/tool_count/lifecycle_status/PID/endpoint 渲染到 TUI timeline；`runtime/tui/src/tests.rs` 的 fake App Bridge 补齐 `/api/mcp`、`/api/mcp/servers/local-tools/start|stop|restart|test`、`PATCH /api/mcp/servers/local-tools`，新增 `app_bridge_terminal_mcp_lifecycle_controls_remote_bridge` keyflow 测试，按真实 TUI 输入路径执行 `/mcp` -> `/mcp start local-tools` -> `/mcp enable local-tools` -> `/mcp test local-tools` -> `/mcp show local-tools` -> `/mcp stop local-tools`，验证 timeline 显示 stopped/running、enabled yes、tools=1、pid=4242、`tools: stdio_echo`，并断言实际 HTTP 请求形状。`tests/golden/rust_rewrite/app_bridge_tui.json` 同步 `/help` golden。验证：`cargo test -p openagent-tui tui_control_matches_legacy_oracle --test tui_control -- --nocapture` 通过；`cargo test -p openagent-tui app_bridge_terminal_mcp_lifecycle_controls_remote_bridge --lib -- --nocapture` 通过；`cargo fmt --all -- --check` 通过；`cargo test -p openagent-tui --lib -- --nocapture` 通过 52 个 tests；`cargo check -p openagent-tui -p openagent-app-server-client` 通过；`git diff --check` 与显式 trailing whitespace/tab 检查通过。残留：TUI 现在有 `/mcp` slash command，但还不是完整 MCP picker/面板；CLI standalone `--mcp-config` direct stdio path 仍未共享 App Bridge lifecycle；lifecycle registry 仍是 App Bridge 进程内状态，不跨 runtime 重启。
  - 2026-07-03: CLI remote MCP lifecycle controls via App Bridge slice 完成。本轮把 MCP lifecycle 控制从 Desktop/UI 与 packaged workflow 继续推进到 CLI/TUI/Desktop 共用的 Rust App Bridge client 路径：`runtime/app-server-client/src/app_bridge_client.rs` 新增 `mcp_status(refresh)`、`mcp_server_test(name)`、`mcp_server_lifecycle(name,start|stop|restart)`、`mcp_server_update(name, body)`，并在 `app_bridge_client_fixture` / `tests/golden/rust_rewrite/app_bridge_tui.json` 记录 request shape；`cli/src/mcp.rs` 新增远端模式，当 `openagent mcp list/show/doctor/test/start/stop/restart/enable/disable` 带 `--server-url`/`--attach` 或使用 lifecycle-only 命令时，复用同一 `RemoteRuntimeClient` 和 bearer/basic auth 访问 `/api/mcp` 与 `/api/mcp/servers/{name}/...`，JSON 模式保留完整 App Bridge payload，text 模式展示远端 MCP 状态表；`cli/src/help.rs` 更新 MCP help；`cli/src/remote.rs` 暴露内部 app bridge client helper 给 mcp 模块。新增 `binary_mcp_remote_lifecycle_controls_app_bridge` 集成测试：启动真实 `openagent serve`，临时 workspace 默认 `.openagent/mcp.json` 内 local stdio MCP server 初始 disabled，通过 CLI 远端 `mcp list` 看到 stopped/disabled，再 `mcp start local-tools` 启动 lifecycle，`mcp enable local-tools` 保持同一 PID，`mcp test local-tools` 发现 stdio tool 且仍是同一 PID，最后 `mcp stop local-tools` 停止。验证：`cargo fmt --all -- --check` 通过；`cargo test -p openagent-app-server-client app_bridge_client_matches_legacy_oracle --test remote_runtime -- --nocapture` 通过；`cargo test -p openagent-cli binary_mcp_remote_lifecycle_controls_app_bridge --test cli_commands -- --nocapture` 通过；`cargo test -p openagent-cli binary_help_smoke_covers_legacy_command_surface --test cli_commands -- --nocapture` 通过；`cargo test -p openagent-app-server-client --lib -- --nocapture` 通过 2 个 tests；`cargo check -p openagent-app-server-client -p openagent-cli` 通过；`git diff --check` 与显式 trailing whitespace/tab 检查通过；无本轮 openagent runtime / stdio MCP / Vite 残留进程。残留：这让 CLI 能控制 App Bridge lifecycle，但 CLI standalone `--mcp-config` direct stdio path 仍是短连接，尚未共享 App Bridge 进程内 lifecycle registry；未做 TUI interactive `/mcp start` slash command，只补 CLI command surface 和 shared client。
  - 2026-07-03: Desktop real local MCP lifecycle UI smoke slice 完成。本轮把 MCP lifecycle 从 packaged API/workflow smoke 继续推进到真实 Desktop UI 路径：新增 `desktop/scripts/smoke-local-mcp-ui.mjs` 和 `desktop/package.json` 的 `smoke:local-mcp-ui`，脚本启动真实 Rust App Bridge + Vite Desktop + headless Chrome，在临时 workspace 写入 disabled local stdio MCP 配置，页面打开详情 MCP 面板后点击 Start，再点击 Enable，然后通过 App Bridge direct `tool_call` 调用 `mcp_tool_local_tools_stdio_echo`，验证 timeline 出现 `[data-testid="mcp-tool-card"]`、右侧 `[data-testid="mcp-latest-call"]` 显示 lifecycle reused / PID / 输出，且 fake MCP request log 只有一个 stdio PID。验证过程中发现并修复真实后端 bug：`mcp_server_fingerprint` 原本把 `enabled` 计入 lifecycle fingerprint，导致 UI 先 Start disabled server、再 Enable 时把同一进程判为 stale，tool call 退回短启动且 UI Lifecycle/PID 为空；现在 fingerprint 会忽略 enabled 开关，因为 enabled 不属于本地子进程启动配置。新增 `app_bridge_mcp_lifecycle_survives_enable_toggle` 回归，覆盖 workspace 默认 `.openagent/mcp.json`、disabled refresh 不启动、start disabled lifecycle、PATCH enabled true、tool call 仍复用同 PID。验证：`node --check desktop/scripts/smoke-local-mcp-ui.mjs` 通过；`npm --prefix desktop run build` 通过；`cargo test -p openagent-http-runtime app_bridge_mcp --lib -- --nocapture` 通过 9 个 MCP tests；`npm --prefix desktop run smoke:local-mcp-ui` 通过，输出 `desktop stdio echo: ui-lifecycle`、`lifecycle_pid=22264`、`request_pid_count=1`、methods 为 initialize/notifications/initialized/tools/list/tools/list/tools/call/shutdown/exit，截图 `/var/folders/6h/_xdqdq9177lcf_s0lf4kt8440000gn/T/openagent-desktop-local-mcp-ui-1783044648899.png`；`cargo fmt --all -- --check`、`git diff --check -- runtime/http/src/http_runtime.rs .goal/state.md progress.md`、显式 trailing whitespace/tab 检查通过；无本轮 runtime/Vite/MCP 残留进程。残留：这是 Vite/Desktop UI + external Rust App Bridge smoke，不是 packaged Tauri GUI click smoke；lifecycle registry 仍是 App Bridge 进程内状态，不跨 runtime 重启；CLI standalone MCP path 仍未复用 Desktop/App Bridge lifecycle。
  - 2026-07-03: Packaged Tauri local MCP lifecycle workflow smoke slice 完成。本轮把真实 packaged `OpenAgent.app` 下的 local stdio MCP lifecycle 从 UI/后端分层能力推进到 packaged workflow 验收：`desktop/scripts/smoke-packaged-app.mjs` 新增 `local-mcp-lifecycle` workflow，会在临时 workspace 写入 `.openagent/mcp.json` 和 fake stdio MCP server，启动 bundled Rust App Bridge 的 `/api/mcp/servers/local-tools/start`，再通过 App Bridge turn `tool_call` 调用 `mcp_tool_local_tools_stdio_echo`，断言 turn event 与 persisted `messages_v2` 中都有 `packaged stdio echo: packaged-lifecycle`，metadata 标记 `mcp_lifecycle_reused=true` 且 PID 一致，并读取 fake MCP 请求日志证明 initialize/tools/list/tools/call/shutdown 全部来自同一个 stdio PID，没有短启动第二个进程。修复过程中补齐两个 Rust runtime 缺口：`register_runtime_mcp_tools` 现在使用和 `/api/mcp` 一致的 MCP config source，并支持 session workspace 默认 `.openagent/mcp.json`；`refresh_mcp_manager_server_tools` 对 local stdio server 会优先复用 running lifecycle session，避免 Desktop/UI 背景 refresh 再短启动 discovery。`desktop/package.json` 新增 `smoke:packaged-app:local-mcp-lifecycle`。验证：`cargo fmt --all -- --check` 通过；`cargo test -p openagent-http-runtime app_bridge_mcp --lib -- --nocapture` 通过 8 个 MCP tests；`npm --prefix desktop run build` 通过；`npm --prefix desktop run tauri -- build --bundles app` 通过并重新打包 `OpenAgent.app`；direct packaged smoke 通过，workflow request methods 为 initialize/notifications/initialized/tools/list/tools/list/tools/call/shutdown/exit 且 `request_pid_count=1`；LaunchServices packaged smoke 通过，同样 `request_pid_count=1`、`lifecycle_reused=true`、输出正确。`git diff --check -- runtime/http/src/http_runtime.rs .goal/state.md progress.md` 通过；无本轮 OpenAgent runtime/Desktop/packaged MCP 残留进程。残留：screenshot artifact 仍因当前 macOS 前台窗口不是 OpenAgent 被降级为 `null`，不影响 API/workflow 验收；lifecycle registry 仍是 App Bridge 进程内状态，不跨 runtime 重启；CLI standalone MCP path 仍未复用 Desktop/App Bridge lifecycle。
  - 2026-07-02: Desktop MCP lifecycle trace visibility slice 完成。本轮把上一段后端新增的 `mcp_lifecycle_reused` / `mcp_lifecycle_pid` metadata 接到 Desktop 可视层：`desktop/src/App.tsx` 的 `McpToolTrace` 增加 `lifecycleReused` 与 `lifecyclePid`，解析 tool part metadata 中的 MCP lifecycle 字段；timeline 的 MCP tool card trace strip 会显示 `lifecycle reused` 和 `pid <pid>`；右侧 MCP inspector 的 `Latest call` 增加 Lifecycle/PID 两行，能直接看到最近一次 MCP tool call 是否复用了已启动的 local stdio lifecycle session。验证：`npm --prefix desktop run build` 通过；inline Node fake App Bridge + Playwright rendered smoke 通过，fake persisted `messages_v2` 返回带 `mcp_lifecycle_reused:true`、`mcp_lifecycle_pid:4242` 的 MCP tool part，浏览器中 `[data-testid="mcp-tool-card"]` 显示 `MCP: stdio_echo`、`lifecycle reused`、`pid 4242`，打开 details 后 `[data-testid="mcp-latest-call"]` 显示 `Lifecycle reused` 和 `PID 4242`；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css .goal/state.md progress.md` 与显式 trailing whitespace/tab 检查通过；进程检查无本轮 Vite/Node 残留，只看到既有 sub2api 前端 Vite。残留：这是 fake App Bridge 渲染 smoke，不是真实 packaged Tauri local MCP UI workflow；`desktop/` 仍是 untracked 目录，未改变 git 跟踪状态；CLI standalone MCP path 仍未复用 App Bridge lifecycle。
  - 2026-07-02: Agent MCP lifecycle session reuse slice 完成。本轮把 App Bridge 运行中的 local stdio MCP lifecycle session 接入 Agent/runtime MCP tool 执行路径：`register_runtime_mcp_tools` 现在优先通过 `refresh_mcp_lifecycle_server` 复用已启动的 local stdio server 来注册 provider-visible MCP tools；`execute_runtime_mcp_tool` 在 local stdio + running lifecycle session 存在时直接用同一个 `StdioMcpSession::request("tools/call", ...)` 执行工具，不再走 `mcp_json_rpc` 短启动路径，并在 tool result metadata 写入 `mcp_lifecycle_reused=true` 与 `mcp_lifecycle_pid`。测试 fake stdio MCP server 增加 `tools/call` 响应和可选 `LOCAL_REQUEST_LOG`，新增 `app_bridge_mcp_tool_call_reuses_local_stdio_lifecycle_session`：先 `POST /api/mcp/servers/local-tools/start`，再通过 App Bridge turn 的 `tool_call` 调用 `mcp_tool_local_tools_stdio_echo`，断言输出 `stdio echo: from-lifecycle`、metadata 标记 lifecycle 复用、`/api/mcp` 仍显示同一 pid，并读取请求日志证明 initialize/tools/list/tools/call 全部来自同一个 stdio PID，没有另起短连接。验证：`cargo fmt --all` 通过；`cargo test -p openagent-http-runtime app_bridge_mcp_tool_call_reuses_local_stdio_lifecycle_session --lib -- --nocapture` 通过；`cargo test -p openagent-http-runtime app_bridge_mcp --lib -- --nocapture` 通过 8 个 MCP tests；`cargo test -p openagent-mcp --tests -- --nocapture` 通过 3 个 MCP crate tests；`cargo check -p openagent-http-runtime -p openagent-mcp` 通过；`git diff --check -- runtime/http/src/http_runtime.rs .goal/state.md progress.md` 和显式 trailing whitespace/tab 检查通过。残留：registry 仍是 App Bridge 进程内状态，不跨 runtime 重启；CLI standalone MCP path 仍是短连接；未做真实 packaged Tauri local MCP UI smoke。
  - 2026-07-02: Desktop MCP lifecycle controls slice 完成。本轮把上一段 Rust App Bridge MCP local stdio lifecycle API 接到 Desktop MCP 面板：`desktop/src/App.tsx` 的 `McpServerSummary` 增加 `lifecycle_status`、`lifecycle_pid`、`lifecycle_started_at`、`lifecycle_last_refreshed_at`、`lifecycle_tool_count`；server row 对 local stdio server 显示 lifecycle 状态、pid、started time、runtime tools，并新增 Start/Stop/Restart 图标按钮，分别调用 `POST /api/mcp/servers/{name}/start|stop|restart`；Test/Enable/Delete 同步补 `aria-label`，便于真实可访问交互和 Playwright 定位。`desktop/src/styles.css` 修正 MCP row action 区按钮 spacing，并增加 lifecycle strip 样式，避免新增运行态信息撑乱卡片。验证：`npm --prefix desktop run build` 通过；inline Node fake App Bridge + Playwright smoke 通过，浏览器展开 details 后初始看到 local MCP server，点击 Start 后 fake bridge 收到 `POST /api/mcp/servers/local-tools/start` 且 UI 显示 `Stdio Echo` 与 `pid 4242`，点击 Stop 后收到 stop 且 UI 显示 `pid -`，点击 Restart 后收到 restart 且 UI 显示 `pid 4243`。残留：本轮只接 Desktop UI，不改 Rust lifecycle registry 语义；registry 仍不跨 App Bridge 重启；Agent MCP tool call 仍未复用 running stdio lifecycle session；还没有 packaged Tauri 真实 local MCP UI smoke。
  - 2026-07-02: MCP local stdio lifecycle API slice 完成。本轮把 local stdio MCP 从“一次性 test/discovery”推进到 App Bridge 进程内 lifecycle：`src/mcp/src/mcp_bridge.rs` 暴露可复用 `StdioMcpSession::start/request/tools_list/running/pid/close`，保留 initialize -> initialized -> tools/list -> shutdown/exit 协议；`runtime/http/src/http_runtime.rs` 新增进程内 MCP lifecycle registry，支持 `POST /api/mcp/servers/{name}/start|stop|restart`，`GET /api/mcp` payload 对 local server 增加 `lifecycle_status`、`lifecycle_pid`、`lifecycle_started_at`、`lifecycle_last_refreshed_at`、`lifecycle_tool_count`。`refresh/test` 会优先复用 running stdio session，配置变更或进程退出会清理 registry。新增 `app_bridge_mcp_local_stdio_lifecycle_start_stop_restart`：临时 rustc fake stdio MCP server，验证 start 后 running/pid/tools 可见，普通 `GET /api/mcp` 保持 running 状态，`refresh=true` 复用同一 pid，stop 后 stopped/tool_count 0，restart 后重新 running，并断言不泄露 env secret。验证：`cargo fmt --all -- --check` 通过；`cargo test -p openagent-http-runtime app_bridge_mcp --lib -- --nocapture` 通过 7 个 MCP tests；`cargo test -p openagent-mcp --tests -- --nocapture` 通过 3 个 MCP crate tests；`cargo check -p openagent-mcp -p openagent-http-runtime` 通过；`git diff --check` 和显式 trailing whitespace/tab 检查通过；无本轮 fake stdio/OpenAgent runtime 残留进程，`pgrep` 只看到既有 sub2api Vite。残留：registry 仍是 App Bridge 进程内状态，不跨 runtime 重启；Agent tool call 仍使用 `mcp_json_rpc` 短连接，没有复用 lifecycle session；Desktop MCP 面板还没接 start/stop/restart 按钮；没有 packaged app 真实 local MCP smoke。
  - 2026-07-02: MCP local stdio discovery/test slice 完成。本轮把上一段 local stdio 配置编辑推进到真实 App Bridge 连通验证：`src/mcp/src/mcp_bridge.rs` 的 stdio MCP session 现在 pipe stderr，并在 initialize/tools/list 超时、stdout 关闭或 frame 读取失败时把截断后的 stderr 摘要附到错误消息中，Desktop/Test 面板能看到本地 server 启动失败原因；成功路径仍按 MCP stdio frame 执行 initialize -> notifications/initialized -> tools/list -> shutdown/exit。`runtime/http/src/http_runtime.rs` 新增 `app_bridge_mcp_refresh_and_test_discover_local_stdio_tools`，测试会临时用 `rustc` 编译一个 fake stdio MCP server，配置为 local command + args + relative cwd + env，然后分别通过 `GET /api/mcp?refresh=true` 和 `POST /api/mcp/servers/local-tools/test` 真实拉起该本地 stdio server，发现 `stdio_echo` tool，并断言 `/api/mcp` payload 不泄露 `LOCAL_SECRET` / env value。验证：`cargo fmt --all -- --check` 通过；`cargo test -p openagent-http-runtime app_bridge_mcp --lib -- --nocapture` 通过 6 个 MCP tests；`cargo test -p openagent-mcp --tests -- --nocapture` 通过 3 个 MCP crate tests；`cargo check -p openagent-mcp -p openagent-http-runtime` 通过；`git diff --check` 和显式 trailing whitespace/tab 检查通过；无本轮 fake stdio/OpenAgent runtime 残留进程，`pgrep` 只看到既有 sub2api Vite。残留：这是一次性 discovery/test，不是 MCP 常驻进程池；每次 refresh/test 仍会短启动本地 server；还没有 start/stop/reconnect daemon、健康缓存、OAuth/插件市场或 packaged app 真实 local MCP smoke。
  - 2026-07-02: MCP local stdio config editor slice 完成。本轮把 Desktop MCP 配置表单从 remote-only 扩成 mode-aware Remote/Local 编辑器：Remote 继续支持 URL/transport；Local 支持 command、args、cwd；两种模式都支持 env、headers、timeout_ms，并在提交前做 name/url/command/timeout/env/header 基础校验。Rust App Bridge MCP config CRUD 已能接收 local stdio 字段并写入默认 workspace `.openagent/mcp.json`，新增回归覆盖 local server 的 command/args/cwd/env/headers/timeout 落盘，同时 `/api/mcp` 返回继续脱敏，不暴露 env/header key/value。验证：`cargo fmt --all -- --check` 通过；`cargo test -p openagent-http-runtime app_bridge_mcp --lib -- --nocapture` 通过 5 个 MCP tests，包含 `app_bridge_mcp_server_config_crud_writes_local_stdio_fields`；`npm --prefix desktop run build` 通过；inline Node + Playwright fake App Bridge smoke 通过，浏览器切到 Local stdio，提交 `local-tools` / `node` / `server.js --stdio` / `/tmp/local-tools` / env/header/timeout，fake bridge 收到精确 JSON `{"name":"local-tools","type":"local","command":"node","args":["server.js","--stdio"],"cwd":"/tmp/local-tools","env":{"LOCAL_SECRET":"local-secret-value"},"headers":{"X-Local-Token":"local-header-secret"},"timeout_ms":3000,"enabled":true}`。残留：这是 local stdio 配置编辑和落盘，不是 MCP 子进程 lifecycle；还没有 start/stop/reconnect daemon、进程保活、真实 local MCP server packaged smoke、OAuth 或插件市场安装。
  - 2026-07-02: MCP per-server health/test slice 完成。本轮在上一段 MCP config CRUD 基础上补齐单 server 手动 Test/Reconnect 闭环：Rust App Bridge protocol manifest 新增 `POST /api/mcp/servers/{name}/test`，动态路由会加载当前 MCP config，定位指定 server，然后复用 `discover_mcp_server_tools` / `build_tool_descriptors_from_values` / `sanitize_mcp_status_error` 做一次显式 tools/list 探测并返回脱敏后的 `/api/mcp` payload；该 test 不写配置，且允许 disabled server 被用户显式测试，server row 可显示 `connected/tools/last_refreshed_at`，全局 enabled 状态仍保持 false。`refresh_mcp_manager_tools(...)` 拆为全量 refresh + 单 server refresh helper，避免 `/api/mcp?refresh=true` 与单 server test 逻辑漂移。Desktop MCP server row 新增 Test 按钮（RefreshCw icon），点击后调用 `/api/mcp/servers/{name}/test`，成功后更新 row 状态、tools 和 Checked 时间；同时修正 MCP row action 区 CSS，避免文本区 grid 样式覆盖按钮布局，并把 server meta 从 4 列扩到 5 列展示 Checked。验证：`cargo fmt --all -- --check` 通过；`cargo test -p openagent-http-runtime app_bridge_mcp --lib -- --nocapture` 通过 4 个 MCP tests，新增 disabled server 手动 test 发现 `lookup` tool 且不泄露 `test-secret`；`npm --prefix desktop run build` 通过；inline Node + Playwright fake App Bridge smoke 通过，浏览器展开 details 后点击 `Test MCP server`，fake bridge 收到 `POST /api/mcp/servers/remote-tools/test`，MCP row 显示 `connected`、`Lookup` 和 `Checked`，console/page errors=0；`git diff --check`、`cargo fmt --all -- --check` 和显式 trailing whitespace/tab 检查通过；本轮无残留 OpenAgent runtime/Desktop/smoke/node 进程，`pgrep` 只看到既有 sub2api Vite 与 william-kb Vite preview。残留：这是手动 health/test，不是 MCP 常驻进程 lifecycle；local stdio server 的 command/args/env/header 编辑仍很薄；未做 OAuth、插件市场安装、真实 remote MCP packaged smoke。
  - 2026-07-02: MCP server config CRUD slice 完成。本轮把 MCP 管理从只读 inspector 推进到可写配置的第一段闭环：Rust App Bridge 新增 `POST /api/mcp/servers`、`PATCH /api/mcp/servers/{name}`、`DELETE /api/mcp/servers/{name}`，`GET /api/mcp` 新增 `writable`、`config_path`、`readonly_reason`，无显式配置时默认写入 workspace `.openagent/mcp.json`；显式路径配置可写，inline JSON 配置保持只读，避免误改命令行字面量。mutation 先读 JSON、按现有 `mcpServers` / `mcp.servers` / direct object 形态定位 servers，再复用 `load_mcp_config_from_value` 校验后落盘，返回的 MCP payload 继续只暴露脱敏状态，不泄露 URL token/header/env。Desktop MCP card 新增紧凑 Name/URL/Transport 表单和 server row enable/disable/delete 图标按钮，成功后直接刷新列表，错误仅显示在 MCP card 内。验证：`cargo fmt --all -- --check` 通过；`cargo test -p openagent-http-runtime app_bridge_mcp --lib -- --nocapture` 通过 3 个 MCP tests，新增 default config add->disable->delete 文件落盘测试并断言 `crud-secret` 不出现在 `/api/mcp` payload；`npm --prefix desktop run build` 通过；inline Node + Playwright fake App Bridge smoke 通过，浏览器打开 Desktop、展开 details、MCP 表单新增 `remote-tools`、禁用、删除，fake bridge 收到 `POST /api/mcp/servers`、`PATCH /api/mcp/servers/remote-tools`、`DELETE /api/mcp/servers/remote-tools`，console/page errors=0；`git diff --check` 和显式 trailing whitespace 检查通过；无本轮残留 runtime/Desktop/Vite/smoke/node 进程，仅看到既有 sub2api 前端 Vite。残留：这是配置 CRUD，不是 MCP 进程 lifecycle 常驻管理；local stdio server 的命令/参数 UI 仍很薄；未做 OAuth、插件市场安装、真实 remote MCP server packaged smoke。
  - 2026-07-02: Packaged Tauri real Sub2API streaming smoke slice 完成。本轮把 packaged `.app` 验证从 fake provider approval/rollback 推进到真实公网 Sub2API streaming：真实 `OpenAgent.app` 通过 LaunchServices 启动，Tauri shell 托管 bundled Rust App Bridge，App Bridge 使用 OpenAI-compatible Responses wire API 调用 `http://47.116.192.3/v1` 的 `gpt-5.4-mini`，并证明 SSE delta、turn completion、message persistence 都打通；不改 provider 配置、不输出 API key、不改 runtime 语义、不做签名/DMG/auto-update、不推 GitHub。Provider config preflight 未打印 key，只报告 `api_key:set(len=67)`；本地 env 默认模型显示为 `gpt-5.5`，但 smoke 命令显式覆盖为 `gpt-5.4-mini`。验证：`npm run tauri -- build --bundles app` 通过，刷新 packaged `desktop/src-tauri/target/release/bundle/macos/OpenAgent.app`；`npm run smoke:packaged-app:real-streaming` 通过，LaunchServices 启动 packaged app，bundled bridge `/Contents/Resources/openagent-http-runtime` 健康检查 `auth_required:true/service:openagent-http-runtime/ui_enabled:false`，provider health `healthy:true/model:gpt-5.4-mini/model_count:18/configured_model_available:true/base_url:http://47.116.192.3/v1/api_key:set`，provider config summary 仅显示 `api_key:set(len=67)`；real streaming workflow 生成 session `session_1782982181196_39013_0` 和 turn `turn_1782982181200_39013_1`，elapsed `3450ms`，persisted messages count `2`，assistant text 为 `OA_PACKAGED_REAL_STREAM_BEGIN\nOA_PACKAGED_REAL_STREAM_END`，begin/end markers 均为 true，events summary `event_count:15`、methods `turn/started` / `item/agentMessage/delta` / `turn/completed`、`delta_count:13`、`completed_count:1`、`failed_count:0`、`turn_model:gpt-5.4-mini`。Screenshot artifact 仍为 `null`，原因同上一轮：当前自动化环境里 OpenAgent 窗口截图失败且前台仍为 Codex；主 workflow/API/session/events 验收不受影响。`desktop/src-tauri/gen/schemas/` 构建产物已清理；无残留 OpenAgent runtime/Desktop/smoke/node 进程；`git diff --check` 和 trailing whitespace 检查通过。残留：这是 real provider streaming smoke，不覆盖 approval/diff/checkpoint rollback；rollback 已由上一轮 fake provider packaged workflow 覆盖；截图 artifact 仍受当前 macOS 前台/截图权限限制；macOS 签名、DMG、auto-update、Windows packaged app 仍未覆盖。
  - 2026-07-02: Packaged Tauri approval/rollback workflow smoke slice 完成。本轮把 Desktop 验证从 Vite/web preview 推进到真实 packaged `.app`：通过 LaunchServices 启动 `OpenAgent.app`，由 Tauri shell 托管 bundled Rust App Bridge，再走 provider streaming -> tool approval -> file write -> diff -> checkpoint restore -> rollback 的主闭环；不改 Rust runtime/agent loop 语义、不接真实 Sub2API、不做签名/DMG/auto-update、不推 GitHub。`desktop/scripts/smoke-packaged-app.mjs` 的 `captureScreenshot(...)` 对 macOS `screencapture -l<window>` 失败增加降级处理：如果窗口级截图失败且 OpenAgent 不能被自动置前，脚本输出 warning 并返回 `screenshot: null`，不再让截图权限/前台窗口限制掩盖已经通过的 packaged workflow 断言；如果窗口级截图失败但 OpenAgent 仍是前台，则继续尝试全屏截图作为 artifact。验证：`npm run build` 通过；`npm run tauri -- build --no-bundle` 通过，重新构建 release `openagent-desktop`；`npm run tauri -- build --bundles app` 通过，刷新 `desktop/src-tauri/target/release/bundle/macos/OpenAgent.app`；`npm run smoke:packaged-app:workflow` 通过，LaunchServices 启动 packaged app，bundled bridge `/Contents/Resources/openagent-http-runtime` 健康检查 `auth_required:true/service:openagent-http-runtime/ui_enabled:false`，fake OpenAI-compatible provider 收到 2 次 `/v1/responses` 和 2 次 `/v1/models` 请求，workflow 生成 session/turn 并触发 `approval_call_packaged_workflow_write`，allow 后写入 `workflow.txt` 内容 `approved packaged workflow\n`，diff 包含 `workflow.txt`，checkpoint restore 使用 `ckpt_1782981912502_1`，restore 后文件不存在，事件包含 `turn/started`、`item/agentMessage/delta`、`item/toolCall/started`、`turn/approval_requested`、`turn/approval_resolved`、`item/toolCall/completed`、`patch/detected`、`turn/completed`、`checkpoint/restored`。smoke 输出 `screenshot:null`，原因是当前自动化环境中窗口截图失败且前台仍为 Codex；该 warning 已记录，不影响主 workflow 验收。`git diff --check`、显式 trailing whitespace 检查通过；无残留 OpenAgent runtime/Desktop/smoke/node 进程；Tauri 生成的 `desktop/src-tauri/gen/schemas/` 已清理。残留：这是 fake provider 下的 packaged approval/rollback workflow，不是公网 Sub2API real streaming packaged smoke；当前环境无法稳定把 OpenAgent 置为前台截图，视觉 screenshot artifact 为空；macOS 签名、DMG、auto-update、Windows packaged app 仍未覆盖。
  - 2026-07-02: Desktop scheduler lifecycle UI slice 完成。本轮把 Rust App Bridge 已有的 scheduler hardening 状态接到 Desktop 产品面：queued timeout、expired queued turns、recovered durable queue、stale lease takeover 不再只是 API 字段，而是在 Jobs inspector 中可见、可解释；不改后端调度语义、不做 queue priority / running timeout / distributed queue、不做 packaged Tauri smoke、不推 GitHub。`desktop/src/App.tsx` 的 `TurnSchedulerSummary` 增加 `turn_queue_timeout_ms`、`expired_queued_turns`；`isTurnJobTerminal(...)` 把 `expired` 纳入 terminal status，避免过期任务被误算成 active/running；`statusClass(...)`、`turnJobStatusLabel(...)`、`turnJobLifecycleMessage(...)` 增加 expired / interrupted / recovered / cancel_requested 语义；Jobs inspector 指标从 Workers/Queued/Durable/Lease 升级为 Workers/Queued/Durable/Timeout/Expired，scheduler strip 展示 recovery、stale lease takeover、job index persisted；selected job detail 增加 Timeout 行，expired job 的 payload 显示 `removed`，并显示 “Queued turn expired after <timeout> without a worker...” 解释；左侧任务列表对 terminal job 展示 `expired/stopped + turn id`。`desktop/src/styles.css` 的 Job metrics 改为 5 栏，并新增 recovered/bad/warn lifecycle note 样式。验证：`npm run build` 通过；inline Node + Playwright rendered smoke 通过，fake App Bridge 返回 running + recovered queued + expired turn jobs，`/api/turns` scheduler 包含 `turn_queue_timeout_ms=1800000`、`turn_queue_lease_stale_ms=45000`、`expired_queued_turns=2`，打开 inspector 后 Jobs card 展示 `1/1` workers、`Timeout 30m`、`Expired 2 pruned`、`1 recovered from disk`、`stale lease takeover 45s`、`job index persisted`，点击 Expired scheduler session 后 selected detail 显示 `expired`、`Payload removed`、`Queued turn expired after 30m without a worker`，browser console warnings/errors=0、page errors=0；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css` 通过；显式 trailing whitespace 和 tab 检查通过；未发现本轮残留 OpenAgent runtime/Desktop/Vite/Playwright/test binary 进程。残留：这是 Desktop scheduler lifecycle UI，不是新的 scheduler policy；running turn timeout/cancel policy、queue priority、SQLite/DB-backed distributed queue 仍是后续任务；smoke 使用 fake App Bridge，没有跑真实 packaged Tauri GUI。
  - 2026-07-02: Desktop typed elicitation types slice 完成。本轮继续补齐 MCP typed elicitation/forms 与 question queue 的 Desktop 产品面：pending question 表单从纯 select/textarea 升级为能保留 option label/value、识别 select/multiselect/boolean/number/integer/text，并在提交前做本地校验；不改 Rust App Bridge question 协议、不做 MCP server 配置编辑、不做 packaged Tauri smoke、不推 GitHub。`desktop/src/App.tsx` 新增 `ElicitationFieldKind`、`ElicitationOption`，把 question draft 从一维字符串改为 `Record<string, string[][]>`，与后端 `answers` 形态对齐；`questionOptions(...)` 支持 `options`、`choices`、`enum`、schema enum 和 array items enum，且保留 option `label/value`；新增类型识别与默认值归一，boolean 的 `false` 视为有效回答，multiselect 去重并提交多个 answer 值；新增 `questionValidationErrors(...)`，提交前校验 required、无效 option、number/integer、minimum/maximum；底部 dock 的快速 Reply 在表单未满足要求时禁用并提示打开详情；`QuestionElicitationForm` 改为按字段类型渲染 select、多选 checkbox group、boolean checkbox、number input、textarea，并显示字段类型和错误。`desktop/src/styles.css` 新增 typed elicitation 的 checkbox group、inline error、number input 样式。验证：`npm run build` 通过；inline Node + Playwright rendered smoke 通过，fake App Bridge 返回一个含 select + multiselect + boolean + integer 字段的 pending question，打开 inspector 后选择 `Apply patch`、勾选 `src/tests`、取消 `Dry run`、设置 `Retry count=4`、点击 Reply，fake App Bridge 收到精确 body `{"answers":[["apply"],["src","tests"],["false"],["4"]]}`，browser console warnings/errors=0、page errors=0；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css` 通过；显式 trailing whitespace 检查通过。残留：这是 Desktop typed elicitation 表单完整化，不是后端 MCP elicitation protocol 扩展；还没有 schema-level pattern/minLength/maxLength/custom validation，也没有真实 packaged Tauri app smoke；MCP server 配置编辑、启停、删除仍是后续任务。
  - 2026-07-02: Desktop typed elicitation form slice 完成。本轮推进 MCP typed elicitation/forms 与 question queue 的 Desktop 产品面：pending question 不再只能一键默认回答，右侧 Trust 面板能把结构化 `questions[]` 渲染成表单字段，并把用户输入序列化成 Rust App Bridge 已支持的 `answers: string[][]`；不改后端 question 协议、不做 MCP server 配置编辑、不做 packaged app smoke、不推 GitHub。`desktop/src/App.tsx` 新增 `ElicitationField` 与 `questionDrafts` state，按 `request_id` 保存 pending question draft；新增 `questionItems(...)`、`questionOptions(...)`、`questionDefaultAnswer(...)`、`questionElicitationFields(...)`，支持 `questions[]`、`tool_input.questions[]`、option label/value、默认值和 required 标记；`respondQuestion(...)` 从一键默认 `questionAnswers(question.question)` 改为读取当前 draft，并提交 `answers: string[][]`，成功或 dismiss 后清理对应 draft；新增 `QuestionElicitationForm`，右侧 Trust 面板 pending question 渲染为 select/textarea 表单，带 options 的字段用 select，无 options 的字段用 textarea。`desktop/src/styles.css` 新增 `.trust-question-form`、`.elicitation-form`、`.elicitation-field` 样式，保持右侧 inspector 的紧凑表单体验。验证：`npm run build` 通过；inline Node + Playwright rendered smoke 通过，fake App Bridge 返回一个含两个结构化字段的 pending question，打开 inspector 后 `pending-question-form` 包含 `Execution mode` 和 `Target file`，选择 `Apply patch`、填写 `src/mcp/src/mcp_bridge.rs`、点击 Reply 后 fake App Bridge 收到精确 body `{"answers":[["Apply patch"],["src/mcp/src/mcp_bridge.rs"]]}`，browser console warnings/errors=0、page errors=0；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css progress.md .goal/state.md` 通过；显式 trailing whitespace 检查通过；未发现本轮残留 OpenAgent runtime/Desktop/Vite/Playwright/test binary 进程。残留：这是 Desktop 对现有 question queue 的 typed form 承接，不是完整 MCP elicitation protocol 后端实现；目前字段类型按 options/select 与 textarea 推断，尚未支持 number/boolean/checkbox/multiselect/schema validation；smoke 使用 fake App Bridge，没有跑真实 packaged Tauri app。
  - 2026-07-02: Desktop MCP server management slice 完成。本轮把右侧 MCP inspector 从“只看总数”推进到 Codex/Zcode 式可操作管理面板雏形：用户可以看到每个 MCP server 的 endpoint、transport、状态、错误、工具列表，并能手动刷新 `/api/mcp`；不做 MCP 配置写入、不做 typed elicitation/forms、不做 packaged app smoke、不推 GitHub。`desktop/src/App.tsx` 的 `McpServerSummary` 增加 `last_refreshed_at` 字段；新增 `refreshMcp()`，独立调用 `/api/mcp?refresh=true`，更新 MCP payload、刷新状态和错误；新增 `mcpEndpointLabel(...)`、`mcpTransportLabel(...)`、`mcpToolLabel(...)` helper；MCP inspector 标题栏增加 `Refresh MCP` 图标按钮，刷新期间显示旋转状态；MCP server 列表从单行摘要升级为 `mcp-server-list`，展示 server type、enabled/disabled、configured/selected transport、endpoint、status、tool count、timeout、headers/env redacted count、last_error；每个 server 展示最多 6 个 tools，包含 title/original/name、dynamic tool id 和 description 摘要。`desktop/src/styles.css` 新增 `.icon-button.mini`、`.spin` animation、`.mcp-server-list`、`.mcp-server-row`、`.mcp-server-heading`、`.mcp-server-meta`、`.mcp-tool-list`、`.mcp-tool-row` 样式，保持右侧 inspector 的紧凑浅色信息密度。验证：`npm run build` 通过；inline Node + Playwright rendered smoke 通过，fake App Bridge 返回两个 MCP servers（remote-tools ok/two tools、local-tools error/no tools），打开 inspector 后 MCP card 包含 `remote-tools`、`auto -> http`、`Echo`、`Search remote docs`、`local-tools`、`stdio server unavailable`，点击 `Refresh MCP` 后 `/api/mcp` request count 增加，browser console warnings/errors=0、page errors=0；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css progress.md .goal/state.md` 通过；显式 trailing whitespace 检查通过；未发现本轮残留 OpenAgent runtime/Desktop/Vite/Playwright/test binary 进程。残留：这是只读 MCP management panel，不支持新增/编辑/删除 MCP server 配置；还没做 MCP typed elicitation/forms，也没有做 real packaged Tauri MCP smoke；`desktop/` 当前仍是 untracked 目录，`git diff` 无法直接展示其中内容。
  - 2026-07-02: Desktop MCP trace card slice 完成。本轮把上一轮后端 MCP approval/resume 闭环接到 Codex/Zcode 式 Desktop 可视层：MCP tool result 不再只是普通 tool 输出，现在 timeline 有专门 MCP tool card，右侧 MCP inspector 会显示最近一次 MCP call；不做 MCP typed elicitation/forms、不做完整 MCP 管理面板、不做 packaged app smoke、不推 GitHub。`desktop/src/App.tsx` 新增 `McpToolTrace`、`mcpToolTraceFromPart(...)`、`mcpToolTracesFromMessages(...)`，从 tool message part 的 `content.metadata` 识别 `backend:"mcp"`、`mcp_server`、`mcp_original_tool_name`、`mcp_transport`、`mcp_tool_name` 和 `mcp_non_text_blocks`；MCP tool part 的标题从 `Tool: mcp_tool_...` 升级为 `MCP: <original tool>`，summary 展示 server / transport / output；MCP tool card 增加 `data-testid="mcp-tool-card"`、MCP trace strip、Server/Tool/Transport/Call/Dynamic/Blocks rows；右侧 MCP inspector 新增 `Latest call` 小卡，展示最近一次 MCP tool、server、transport、call id、status 和输出摘要，使用 `data-testid="mcp-latest-call"` 便于 smoke。`desktop/src/styles.css` 新增 `.part-mcp-tool`、`.mcp-trace-strip`、`.mcp-latest-call` 样式，保持浅色、紧凑、低装饰的 Codex-like inspector 视觉。验证：`npm run build` 通过；inline Node + Playwright rendered smoke 通过，fake App Bridge 返回 `/api/mcp` 和带 MCP metadata 的 persisted `messages_v2` assistant message，页面中 `[data-testid="mcp-tool-card"]` 包含 `MCP: echo`、`remote-tools`、`mcp echo: approved-mcp`，`[data-testid="mcp-latest-call"]` 包含 `Latest call`、`echo`、`http`，browser console warnings/errors=0、page errors=0；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css progress.md .goal/state.md` 通过；显式 trailing whitespace 检查通过；未发现本轮残留 OpenAgent runtime/Desktop/Vite/Playwright/test binary 进程。残留：`desktop/` 当前仍是 untracked 目录，`git diff` 无法直接展示其中内容；这是 persisted message part 的可视化承接，还不是完整 MCP server 管理面板、typed elicitation/forms 或 MCP approval dock 专门交互；smoke 使用 fake App Bridge，没有跑真实 packaged Tauri app。
  - 2026-07-02: MCP approval / resume E2E slice 完成。本轮补齐 Rust App Bridge / Agent Runtime 的 MCP 信任边界验收锁：provider 触发动态 MCP tool call 后，`PLAN_ONLY` 会暂停到 approval queue；用户 allow 后 runtime 会真正调用 MCP `tools/call`，再把 MCP 输出作为 `function_call_output` 喂回 provider 并完成最终回答；不做 Desktop MCP trace card、不改 MCP UI、不推 GitHub。`runtime/http/tests/http_runtime.rs` 新增 `remote_runtime_client_resumes_provider_after_mcp_approval_allow`，覆盖 fake OpenAI Responses provider 发出 `mcp_tool_remote_tools_echo` function_call、runtime 返回 `waiting_approval`、approval payload 指向动态 MCP tool、global approval allow 后清空 `pending_approval` / `pending_provider_turn`、事件流包含 `item/toolCall/completed` 和 `turn/completed`、provider 第二次请求包含 `function_call_output` 与 `mcp echo: approved-mcp`、fake MCP server 收到 `tools/list -> tools/list -> tools/call`。fake MCP server helper 抽为 `spawn_fake_mcp_server_with_limit(max_requests)`，普通 MCP 测试仍使用 2 请求上限，approval/resume 测试使用 3 请求上限。验证：新增 MCP approval/resume E2E 单测通过；`cargo test -p openagent-http-runtime mcp --test http_runtime -- --nocapture` 通过 2 个 MCP tests；普通 provider approval resume regression 通过；`cargo check -p openagent-http-runtime` 通过；`cargo fmt --all -- --check` 通过；`git diff --check -- runtime/http/tests/http_runtime.rs progress.md .goal/state.md` 通过；未发现本轮残留 OpenAgent runtime/Desktop/smoke/test binary 进程，`pgrep` 只看到既有 sub2api 前端 Vite 进程。残留：这是后端 E2E regression，不是 Desktop MCP 工具卡片或 MCP 面板；approval allow 路径会重新做一次 MCP `tools/list` discovery 再执行 `tools/call`，后续可加短期缓存；MCP typed elicitation/forms 和 Desktop MCP trace card 仍未完成。
  - 2026-07-02: Queue timeout / expiry slice 完成。本轮推进 Codex/Zcode 式 App Bridge scheduler hardening：queued async turn 现在不会无限等待；超过配置 timeout 后会进入 terminal `expired` 状态，并清理 durable queue payload 与 owner lease；不做 Desktop 新 UI、不做 queue priority、不做 distributed scheduler、不推 GitHub。`runtime/http/src/http_runtime.rs` 的 `HttpRuntimeConfig` 新增 `turn_queue_timeout_ms`，默认 `30 * 60 * 1000` ms，支持 `OPENAGENT_TURN_QUEUE_TIMEOUT_MS` 和 CLI `--turn-queue-timeout-ms` / `--queue-timeout-ms` 覆盖，最小值为 `1`；新增 `queued_turn_expired(...)`、`expire_queued_turns(...)`、`expire_queued_turns_locked(...)`，根据 queued payload 的 `queued_at_ms` 判断过期；`pop_next_schedulable_queued_turn(...)` 在调度扫描前清理过期项，被 pop 出来但已过期的 queued turn 也会标记 `expired`，不会 promote 为 running；`recover_persisted_queued_turns(...)` 启动恢复时会跳过并标记已过期的 durable queued payload；`list_turn_jobs_payload(...)` 和 `turn_status_payload(...)` 触发 expiry，用户刷新 `/api/turns` 或 `/api/turns/{turn_id}` 即可看到 `expired`；`expired` 纳入 terminal status；async turn accept / queue-full / list scheduler metadata 增加 `turn_queue_timeout_ms`，list scheduler 增加本次刷新触发的 `expired_queued_turns`。`runtime/http/tests/http_runtime.rs` 新增 `async_turn_expires_queued_turn_after_timeout`，覆盖第一条 async turn 占用 session worker，第二条 queued，timeout=150ms 后刷新 `/api/turns`，第二条变为 `expired`，queued payload 文件被删除，`GET /api/turns/{id}` 返回 `expired`，provider 只收到第一条请求；测试 helper 清理 `OPENAGENT_TURN_QUEUE_TIMEOUT_MS`。验证：`cargo fmt --all` 通过；新增 expiry regression 通过；`cargo test -p openagent-http-runtime async_turn --test http_runtime -- --nocapture` 通过 8 个 async turn tests；`cargo check -p openagent-http-runtime` 通过；`cargo fmt --all -- --check` 通过；`git diff --check -- runtime/http/src/http_runtime.rs runtime/http/tests/http_runtime.rs progress.md .goal/state.md` 通过；无残留 runtime/Desktop/Vite/fake bridge/test binary 进程。残留：expiry 目前只对 queued-not-yet-running turn 生效；running turn 超时/cancel 仍是后续 worker/runtime policy；`expired` 还没有 dedicated Desktop wording；这是单 runtime/file-based scheduler hardening，不是 SQLite/DB-backed distributed queue。
  - 2026-07-02: Desktop scheduler status UI slice 完成。本轮把最近几轮 App Bridge scheduler 能力接到 Codex/Zcode 式 Desktop 任务可视化：Desktop 现在能展示 global worker quota、queued reason、durable queued payload、lease stale、job index persisted 等状态；queue-full 拒绝也不再只显示原始 HTTP 429；不改 Rust 后端调度语义、不做 queue timeout/expiry、不做 packaged Tauri smoke、不推 GitHub。`desktop/src/App.tsx` 的 `TurnJobSummary` 增加 `queue_reason`、`payload_persisted`，`TurnJobsPayload` 增加 `active_count`、`scheduler`，新增 `TurnSchedulerSummary`；`normalizeTurnJobs(...)` 修正 fallback 计数，running 不再把 queued 算进去；App Bridge `api(...)` 非 2xx 错误现在会保留 JSON body 并抛出 `ApiError`；`submitPrompt(...)` 处理 `turn_queue_full`，显示中文提示 `排队已满：当前会话已有 N/M 个等待任务。` 并刷新 `/api/turns`；optimistic job upsert 保留 `queue_reason`；左侧任务树 queued 行显示 `Queue #N · worker quota/session active`；Jobs inspector 指标改为 Workers/Queued/Durable/Lease，展示 `running_turn_workers/max_running_turn_workers`、durable queued payload 数、lease stale 秒数；新增 scheduler strip：`session queue max N`、`N waiting for worker quota`、`job index persisted`；selected queued job 详情新增 Reason/Payload，并按 `global_worker_quota` / `session_active` 展示不同 waiting note。`desktop/src/styles.css` 新增 scheduler strip 和 quota queue note 样式。验证：`npm run build` 通过；In-app Browser fake App Bridge smoke 通过，页面无 console warn/error，DOM 包含 `Workers 1/1`、`Queued 1`、`Durable 1 saved`、`Lease 30s`、`session queue max 1`、`1 waiting for worker quota`、`job index persisted`，queued job detail 包含 `Reason worker quota`、`Payload persisted`、`Waiting for a runtime worker to free up.`，提交 prompt 后 fake App Bridge 返回 `turn_queue_full`，composer 下方显示 `排队已满：当前会话已有 1/1 个等待任务。`；`awk` trailing whitespace 检查通过；临时 fake App Bridge、Vite dev server 已停止。残留：`desktop/` 目录在当前 worktree 仍是 untracked，`git diff --check` 不覆盖其内容；本轮 smoke 用 fake App Bridge，没有跑 packaged Tauri GUI 或真实 App Bridge queue-full/global quota；还缺 queue timeout/expiry、queue priority、Desktop recovered/stale lease takeover 状态和 MCP approval/resume E2E。
  - 2026-07-02: Global turn worker quota slice 完成。本轮推进 Codex/Zcode 式 App Bridge scheduler 底座：async turn 不再只按单 session 排队，也会受 runtime 全局 worker quota 约束；多个 session 同时提交时，超出全局并发预算的 turn 会进入 durable queue，等任一 worker 释放后自动启动；不做 queue timeout/expiry、不改 Desktop UI、不做 distributed scheduler、不推 GitHub。`runtime/http/src/http_runtime.rs` 新增 `HttpRuntimeConfig.max_running_turn_workers`，默认 `4`，支持 `OPENAGENT_MAX_RUNNING_TURN_WORKERS` 和 CLI `--max-running-turn-workers` / `--max-turn-workers` 覆盖，最小有效值为 `1`；`register_turn_job(...)` 同时检查同 session running turn 和全局 running worker 数，同 session active 时返回 `queue_reason:"session_active"`，全局 quota 满时返回 `queue_reason:"global_worker_quota"`；queued turn payload 增加内存 `queued_at_ms`，跨 session 调度时从各 session 队列 front 里选择最早入队项，保留每个 session 的 FIFO；worker terminal 后调用全局 `start_next_queued_turns(...)`，在 quota 允许时持续启动可运行 queued turn；startup recovery 复用全局调度入口；async turn accept/list/queue-full 的 scheduler metadata 增加 `max_running_turn_workers`，`GET /api/turns` 增加全局 `running_turn_workers`。`runtime/http/tests/http_runtime.rs` 新增 `async_turn_queues_second_session_when_global_worker_quota_full`，覆盖 quota=1 时第一个 session 占用 worker，第二个 session 返回 queued / global_worker_quota，provider 在第一条完成前只收到 1 次请求，第一条完成后第二条自动执行且只执行一次；测试 helper 清理 `OPENAGENT_MAX_RUNNING_TURN_WORKERS` 避免环境污染。验证：`cargo fmt --all` 通过；新增 global quota regression 通过；`cargo test -p openagent-http-runtime async_turn --test http_runtime -- --nocapture` 通过 7 个 async turn tests；`cargo check -p openagent-http-runtime` 通过；`cargo fmt --all -- --check` 通过；`git diff --check -- runtime/http/src/http_runtime.rs runtime/http/tests/http_runtime.rs progress.md .goal/state.md` 通过。残留：这是单 runtime 进程内全局 quota，不是跨机器/多 runtime 的强一致调度；还没有 queue timeout/expiry、queue priority、Desktop queue-full/recovered/leased/global-quota 状态提示或 packaged app restart smoke。
  - 2026-07-02: Queued turn owner lease slice 完成。本轮推进 Codex/Zcode 式 durable scheduler 的跨进程安全边界：queued async turn payload 已能落盘恢复后，本轮给 queued turn 增加 owner lease，避免两个 runtime 指向同一个 session root 时同时恢复并执行同一条 queued turn；不做 running turn replay、不做分布式锁服务、不做全局 worker quota、不改 Desktop UI、不推 GitHub。`runtime/http/src/http_runtime.rs` 新增 `.openagent-runtime/turn_queue_leases/{turn_id}.lease.json` lease schema，包含 `owner_id`、`turn_id`、`claimed_at_ms`、`updated_at_ms`；runtime 进程生成稳定 `owner_id`；queued turn 注册时先原子 `create_new` claim lease，再写 queued payload / job index / memory queue；`claim_queued_turn_lease(...)` 支持 owner 重入、corrupt lease 清理和 stale takeover，默认 stale timeout 为 `30_000ms`，支持 `OPENAGENT_TURN_QUEUE_LEASE_STALE_MS` 和 CLI `--turn-queue-lease-stale-ms` 覆盖；queued payload 被 promote/cancel/terminal 清理时同时释放 lease；startup recovery 只有成功 claim lease 后才会恢复 queued payload，抢不到 live lease 的 runtime 会跳过，不会重复启动；startup recovery 只有在实际 claim 到 queued payload 的 session 上才把旧 non-queued active job 标记为 `interrupted`，避免旁路 runtime 抢不到 lease 也污染 active job index；`/api/turns` 和 async turn accept/queue-full response 的 scheduler block 增加 `turn_queue_lease_stale_ms`。`runtime/http/tests/http_runtime.rs` 更新 restart recovery regression，用 `OPENAGENT_TURN_QUEUE_LEASE_STALE_MS=1` 模拟 owner crash 后 stale takeover；新增 `async_turn_recovery_respects_live_queue_lease_owner`，覆盖第二个 runtime 在 live lease 未过期时启动，不会恢复/执行 queued turn，原 owner runtime 完成第一条后按 FIFO 执行第二条，provider 只收到两次请求。验证：`cargo fmt --all` 通过；`cargo test -p openagent-http-runtime async_turn_recover --test http_runtime -- --nocapture` 通过 2 个 recovery/lease tests；`cargo test -p openagent-http-runtime async_turn --test http_runtime -- --nocapture` 通过 6 个 async turn tests；`cargo check -p openagent-http-runtime` 通过；`cargo fmt --all -- --check` 通过。残留：这是 file-based owner lease，不是数据库/etcd/SQLite style distributed scheduler；running turn 仍不会跨进程 replay；还没有全局 worker quota、queue timeout/expiry、Desktop queue-full/recovered/leased 状态提示或 packaged app restart smoke。
  - 2026-07-02: Durable queued turn payload recovery slice 完成。本轮推进 Codex/Zcode 式 durable scheduler 边界：App Bridge queued async turn 不再只存在于进程内队列，queued payload 会落盘；runtime 重启后可以恢复尚未执行的 queued turn，并继续从队列启动执行；不做跨进程强 lease、不做全局 worker quota、不做 running turn payload replay、不改 Desktop UI、不推 GitHub。`runtime/http/src/http_runtime.rs` 新增 `.openagent-runtime/turn_queue/{turn_id}.json` queued payload schema，记录 `session_id`、`turn_id`、`queued_at_ms` 和原始 async turn payload；queued turn 注册时先写 durable payload，再写内存 job/queue 和 job index，payload 写失败时返回 `turn_queue_persist_failed`；queued turn promote 为 running、queued cancel/interrupted、terminal status 写入时会清理 durable payload 和内存 queue entry；runtime `serve_blocking(...)` 启动后会扫描 durable queued payloads，把仍处于 `queued` 的 job 恢复进内存 FIFO queue，并对每个 session 启动下一条 queued turn；启动恢复时会把旧进程遗留的 non-queued active job 标记为 `interrupted`，防止 stale running 阻塞 queued recovery；`GET /api/turns` 和 `GET /api/turns/{turn_id}` 对 payload 仍存在的 queued job 保持 `queued`，并返回 `payload_persisted:true`。`runtime/http/tests/http_runtime.rs` 新增 `async_turn_recovers_persisted_queued_turn_after_runtime_restart`，覆盖第一条 async turn 运行、第二条 queued 已落盘时 kill runtime，用相同 session root 重启后第二条 queued turn 自动恢复并完成，旧 running turn 标记为 `interrupted`，provider 只收到 first + recovered second 两次请求。验证：`cargo fmt --all` 通过；单独 restart recovery regression 通过；`cargo test -p openagent-http-runtime async_turn --test http_runtime -- --nocapture` 通过 5 个 async turn 测试；`cargo check -p openagent-http-runtime` 通过；`cargo fmt --all -- --check` 通过。残留：只恢复 queued-not-yet-running payload；running turn 不跨进程 replay，重启后标记 `interrupted`；还没有跨进程强 lease/owner token、queue timeout/expiry、全局 worker quota、Desktop queue-full/recovered toast 或 packaged app restart smoke。
  - 2026-07-02: Async turn queue guardrails slice 完成。本轮推进 Codex/Zcode 式任务调度底座：App Bridge async turn queue 不再是无限入队，同一 session 的等待队列有明确上限，超限时返回可机器处理的 HTTP `429` / `turn_queue_full`，CLI/TUI/Desktop 可以共用这个语义做用户提示；不做 durable payload recovery、不做跨进程 worker lease、不做跨 session 并发 quota、不改 Desktop UI、不推 GitHub。`runtime/http/src/http_runtime.rs` 的 `HttpRuntimeConfig` 新增 `max_queued_turns_per_session`，默认 `8`，支持 `OPENAGENT_MAX_QUEUED_TURNS_PER_SESSION` 和 CLI `--max-queued-turns-per-session` / `--max-queued-turns` 覆盖；`register_turn_job(...)` 在同 session 有 running turn 时先检查 queued turn 数量，达到上限时不写入 job registry、不调用 provider，直接返回 queue-full；async `/api/sessions/{session_id}/turns` 超限 payload 包含 `error_code:"turn_queue_full"`、`accepted:false`、`queued:false`、当前 `queued_count` 和 scheduler policy；queued accept response 增加 `queue_position` 和 `scheduler.max_queued_turns_per_session`；`GET /api/turns` 区分 `running_count`、`queued_count`、`active_count`、`terminal_count`，queued jobs 会带 `queue_position`。`runtime/http/tests/http_runtime.rs` 更新 async queue regression，并新增 `async_turn_rejects_when_session_queue_is_full`，覆盖 limit=1 时第一条 running、第二条 queued、第三条 429 且 provider 只收到前两条请求。验证：`cargo fmt --all` 通过；`cargo test -p openagent-http-runtime async_turn --test http_runtime -- --nocapture` 通过 4 个 async turn 测试；`cargo check -p openagent-http-runtime` 通过；`cargo fmt --all -- --check` 通过；`git diff --check -- runtime/http/src/http_runtime.rs runtime/http/tests/http_runtime.rs progress.md .goal/state.md` 通过；无残留 OpenAgent runtime/Desktop/packaged smoke/queued UI smoke 进程。残留：这是 session-level queue guardrail，不是 durable scheduler；runtime 重启后 queued payload 仍不会恢复执行；还缺跨 session 全局 worker quota、持久 worker lease、queue payload 落盘/恢复、queue timeout/expiry 和 Desktop queue-full toast。
  - 2026-07-02: Desktop queued turn UI slice 完成。本轮推进 Codex/Zcode 式任务队列产品面：Desktop 现在能识别 App Bridge `queued` turn 语义，运行中的 session 可以继续提交下一条 prompt 进入队列，并能在任务侧栏/Jobs inspector 看到队列顺序和等待状态；不做 durable queue、不做跨进程 payload 恢复、不做全局 worker quota、不做完整任务 dashboard、不推 GitHub。`desktop/src/App.tsx` 增加 `queue_position` / `queued_count` 类型字段、queued job helpers 和 `turnSubmitState(...)`；`submitPrompt(...)` 消费 async turn 返回的 `queued/status/queue_position`，把 composer 状态更新为 `排队中` 并乐观写入 queued job；active jobs 拆为 running/queued，侧栏显示 `Queue #N`，Jobs inspector 显示 Running/Queued/Recent/Index metrics、`#N waiting` 和 queued wait note；active turn 可中断时，主按钮保留 Stop，新增独立 `Queue prompt` 提交按钮；queued job 详情 Stop 复用 `POST /api/turns/{turn_id}/interrupt`。`desktop/src/styles.css` 新增 queued wait note、4 列 job metrics 和 queue submit button 样式。验证：`npm run build` 通过；fake App Bridge + Playwright queued smoke 通过，初始 running turn 下 composer 显示 Stop + `Queue prompt`，提交后 Desktop 显示 `排队中`、侧栏 `Queue #1`、Jobs inspector `QUEUED`/`#1 waiting`/`queued #1`，点击详情 Stop 命中 interrupt route 恰好 1 次并显示 `已中断`；smoke 无 console warning/error，临时 fake bridge 与 Vite 已停止。残留：这是 Desktop UI 对 in-memory queue 的产品化承接，不是 durable scheduler；runtime 重启后 queued payload 仍不会恢复执行；还缺最大队列长度、跨 session worker quota、持久 worker lease 和完整任务 dashboard。
  - 2026-07-02: Async turn in-memory queue slice 完成。本轮推进 Codex/Zcode 式任务队列语义：同一个 session 的第二个 async turn 不再只返回冲突，而是进入进程内 FIFO 队列，当前 turn 结束后自动 promote 为 running 并执行；不做跨进程恢复执行、不做持久 payload queue、不做多 worker 并发策略、不做 Desktop 队列 UI 精修、不推 GitHub。`runtime/http/src/http_runtime.rs` 新增 `QueuedTurnJob`、`queued_turns()` 和 `turn_scheduler_lock()`，把同 session active 检查、job 插入和 queue push 放在同一调度锁下；`register_turn_job(...)` 在同 session 有 active runner 时创建 `status:"queued"` 的 job，写入 job index，并把 payload 放入进程内 FIFO queue；`start_turn_async_payload(...)` 对 queued turn 返回 HTTP `202`、`accepted:true`、`queued:true`、`status:"queued"`；抽出 `spawn_async_turn_worker(...)`，worker terminal 后调用 `start_next_queued_turn(...)` 自动启动同 session 下一项；queued job 被 interrupt 时标记 `interrupted`，promote 时跳过已取消 job；`new_id(...)` 增加进程内原子序列号，修复快速双请求同毫秒 turn id 碰撞。`runtime/http/tests/http_runtime.rs` 将 conflict test 升级为 `async_turn_queues_second_active_turn_for_same_session`，覆盖第一条 running、第二条 queued、列表中同时展示 running + queued、第一条完成后第二条自动 completed、provider 收到两次请求且顺序正确。验证：`cargo test -p openagent-http-runtime async_turn --test http_runtime -- --nocapture` 通过 3 个 async turn 测试；`cargo check -p openagent-http-runtime` 通过；`cargo fmt --all -- --check` 通过；`git diff --check -- runtime/http/src/http_runtime.rs runtime/http/tests/http_runtime.rs progress.md .goal/state.md` 通过；无残留 OpenAgent runtime/Desktop/packaged smoke 进程。残留：这是 in-memory FIFO queue，不是 durable queue；runtime 重启后 queued payload 不会恢复执行；Desktop 还没专门处理队列顺序/等待原因；还缺全局并发策略、最大队列长度、跨 session worker quota 和持久 worker lease。
  - 2026-07-02: Async turn session concurrency guard slice 完成。本轮推进 Codex/Zcode 式任务运行语义：同一个 session 现在不会同时接受多个 async Agent turn，避免 Desktop/CLI/TUI 连点、重连或重复提交时把一个会话跑成多个并发 provider/tool loop；不做完整 worker queue、不做排队调度、不做跨进程继续执行、不推 GitHub。`runtime/http/src/http_runtime.rs` 的 `register_turn_job(...)` 改为在同一把 runtime job registry mutex 中完成“检查同 session active job + 插入新 job”，当已有非 terminal job 时，async `POST /api/sessions/{session_id}/turns` 返回 HTTP `409`，payload 包含 `error_code:"active_turn_exists"`、`accepted:false`、`existing_turn` 和已有 `turn_id/status`；成功路径仍返回原来的 `202`。`runtime/http/tests/http_runtime.rs` 新增 `async_turn_rejects_second_active_turn_for_same_session`，覆盖第一条 accepted、第二条同 session async turn 409、`GET /api/turns` 仍只有 1 个 running job、provider 只收到第一条请求。验证：`cargo test -p openagent-http-runtime async_turn --test http_runtime -- --nocapture` 通过 3 个 async turn 测试；`cargo check -p openagent-http-runtime` 通过；`cargo fmt --all -- --check` 通过；`git diff --check -- runtime/http/src/http_runtime.rs runtime/http/tests/http_runtime.rs progress.md .goal/state.md` 通过；无残留 runtime/Vite/Desktop/smoke 进程。残留：这是 reject-style concurrency guard，不是完整 queue；后续还需要 worker queue、排队状态、Desktop 队列 UI 和可配置并发策略；同步 turn 路径仍按原有阻塞语义执行。
  - 2026-07-02: Desktop jobs inspector slice 完成。本轮推进 Codex/Zcode 式任务可观察体验：Desktop 不再只在左侧 sidebar 显示一行 active job，右侧 inspector 现在有 Jobs 面板，可以查看 running/recent job 详情、durable index 状态、时间、cancel 状态和该 turn 的 live trace，并能从详情里中断 job；不做完整 job dashboard、不做 durable worker queue、不做跨进程继续执行、不推 GitHub。`desktop/src/App.tsx` 的 `TurnJobsPayload` 增加 `index_persisted`，新增 `selectedTurnJobId` 和 selected job 派生状态；左侧任务点击会切换 active session、聚焦 job、打开右侧 inspector；右侧新增 `data-testid="jobs-inspector-card"`，展示 running/recent/index metrics、选中 job status、turn/session、started/updated/duration、cancel 状态、Stop/Refresh 操作和最近 turn trace event；详情 Stop 复用 `interruptTurn(...)`。`desktop/src/styles.css` 新增 jobs inspector metrics/detail/trace/recent list 样式。验证：`npm run build` 通过；fake App Bridge + Playwright smoke 通过，Desktop 拉取 `/api/turns`，点击左侧 `turn_running` 后打开右侧 Jobs inspector，显示 `Index persisted` 和选中 turn，点击详情 Stop 命中 `POST /api/turns/turn_running/interrupt` 恰好 1 次并更新为 interrupted；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css progress.md .goal/state.md` 通过；无残留 runtime/Vite/Desktop/smoke 进程。残留：这是轻量 Jobs inspector，不是完整任务 dashboard；还没有多任务队列、并发 quota、可恢复 worker 执行或 job search/filter；trace 只展示前端已加载 live events。
  - 2026-07-02: Turn job persistent index slice 完成。本轮推进 Codex/Zcode 式任务可恢复体验：App Bridge turn job registry 不再只存在于 runtime 进程内，async turn 状态会落到 `.openagent-runtime/turn_jobs.json`，runtime 重启后 `GET /api/turns/{turn_id}` 和 `GET /api/turns` 可以从索引恢复最近 job 状态；不做完整 durable worker queue、不做跨进程继续执行、不做 job dashboard、不推 GitHub。`runtime/http/src/http_runtime.rs` 新增 `TurnJobSnapshot` 和持久化 index schema，`register_turn_job`、`mark_turn_job_status`、`request_turn_job_cancel` 会同步更新内存 registry 与 index；`GET /api/turns` 会合并 index 与内存 jobs，发现重启后遗留的非 terminal job 会标记为 `interrupted`，并支持 7 天 terminal TTL prune 与最多 200 条 index 限制；`GET /api/turns/{turn_id}` 优先读内存，缺失时读 index，再回退 session store。`runtime/http/tests/http_runtime.rs` 扩展 async interrupt regression，覆盖 index 落盘、runtime 重启恢复、旧 terminal prune、事件回放仍含 delta/interrupted 且不含 completed。验证：`cargo test -p openagent-http-runtime async_turn_interrupt_cancels_provider_stream_before_completion --test http_runtime -- --nocapture` 通过；`cargo check -p openagent-http-runtime` 通过；`cargo fmt --all -- --check` 通过；`git diff --check -- runtime/http/src/http_runtime.rs runtime/http/tests/http_runtime.rs progress.md .goal/state.md` 通过；无残留 runtime/Vite/Desktop/smoke 进程。残留：这是 durable job index，不是完整 durable scheduler；runtime 重启后恢复状态但不会继续执行旧 worker；provider HTTP cancel 仍是 cooperative。
  - 2026-07-02: Desktop active jobs sidebar slice 完成。本轮推进 Codex/Zcode 式任务树体验：Desktop 现在消费 `GET /api/turns`，左侧 sidebar 新增“任务”小节，可以展示 active/recent turn job，并能直接从任务行中断可运行 job；不做 durable scheduler、不做完整任务管理 dashboard、不改 Rust runtime、不推 GitHub。`desktop/src/App.tsx` 新增 `TurnJobSummary` / `TurnJobsPayload` 类型和 job helper，用于判断 terminal/interruptible、标准化 payload、乐观 upsert job；新增 `turnJobs` state 和 `refreshTurnJobs()`，主 `refresh()`、SSE `refreshFromEvents()`、prompt submit 后 scheduled refresh 都会同步 `/api/turns`；prompt submit 收到 async `turn_id` 后乐观插入 running job；`interruptActiveTurn` 拆为通用 `interruptTurn(turnId)`，composer stop 按钮和 sidebar job stop 按钮复用同一 interrupt 路径；左侧 rail 新增 `data-testid="turn-jobs-section"` 的“任务”小节，running job 显示 session label、turn id 和 stop icon，terminal job 显示 completed/failed/interrupted 状态。`desktop/src/styles.css` 新增 job row/job stop button/job status 样式。验证：`npm run build` 通过；fake App Bridge + Playwright smoke 通过，Desktop 初始拉取 `GET /api/turns`，sidebar “任务”小节显示 `Jobs Smoke` running job 和 stop button，点击 stop 后命中 `POST /api/turns/turn_job_running/interrupt` 恰好 1 次，UI 更新为 `interrupted` 且 stop button 消失；smoke 中 `/api/turns` 被拉取 5 次，无 console warning/error；`git diff --check` 与显式 whitespace 检查通过；无残留 Vite/App Bridge/Desktop/smoke 进程。残留：Desktop 现在能消费进程内 job list，但 runtime job registry 仍未持久化；侧栏任务小节是轻量产品入口，还不是完整 job dashboard；后续仍需要 durable job index、TTL/prune、job detail panel、并发 turn 约束。
  - 2026-07-02: App Bridge turn job list API slice 完成。本轮推进 Codex/Zcode 式运行中任务可观测性：App Bridge 不再只能按 `turn_id` 查单个 turn 状态，现在有统一 `GET /api/turns` 列表 API，CLI/TUI/Desktop 可以共用它来展示 active/interrupted/completed job；不做跨进程 durable worker queue、不做 Desktop job 面板、不做并发 quota、不推 GitHub。`runtime/http/src/http_runtime.rs` 新增 `GET /api/turns`，返回当前 runtime job registry 的 `turns`、`count`、`running_count`、`terminal_count`、`filters` 和 `source`，支持 `session_id`/`session`、`status`、`active=true` 查询过滤，protocol manifest 补充 `GET /api/turns`。`runtime/app-server-client/src/app_bridge_client.rs` 新增 `RemoteRuntimeClient::turns()` 与 `turns_for_session(...)`，fixture request shapes 增加 `/api/turns?session_id=session_existing`；`tests/golden/rust_rewrite/app_bridge_tui.json` 同步 golden。`runtime/http/tests/http_runtime.rs` 在 async interrupt regression 中新增列表 API 断言：accepted 后 `running_count=1` 且 turn 出现在列表；interrupt 后 `running_count=0`、`terminal_count=1`、status 为 `interrupted` 且 `cancel_requested=true`。验证：`cargo test -p openagent-http-runtime async_turn_interrupt_cancels_provider_stream_before_completion --test http_runtime -- --nocapture` 通过；`cargo test -p openagent-app-server-client --lib -- --nocapture` 通过；`cargo test -p openagent-app-server-client --test remote_runtime -- --nocapture` 通过；`cargo check -p openagent-http-runtime -p openagent-app-server-client` 通过；`cargo fmt --all -- --check` 通过；`git diff --check` 通过。残留：这是 runtime 进程内 job registry 的列表 API，不是完整 durable scheduler；runtime 重启后列表不会恢复历史 jobs；terminal jobs 目前留在内存中，后续需要 TTL/prune 或持久化索引；Desktop 还没有消费 `/api/turns` 做 active job 面板。
  - 2026-07-02: Desktop active turn interrupt UI slice 完成。本轮推进 Codex/Zcode 式 Desktop 交互：composer 在 turn 运行时从发送按钮切换为停止按钮，用户可以从 UI 直接中断当前 active turn；不做 durable queue、不做复杂 job dashboard、不改 CLI/TUI、不推 GitHub。`desktop/src/App.tsx` 将 `turn/interrupted` 映射为独立 `interrupted` stream state，新增 `activeTurnIdFromEvents(...)` 从 `turn/started`、stream delta、tool/approval/question 事件中识别当前 active turn，并在 terminal event 后清空；composer submit 在 async `/turns` 返回 `turn_id` 后记录 `activeTurnId`；新增 `interruptActiveTurn(...)` 调用 `POST /api/turns/{turn_id}/interrupt`，合并返回事件，刷新 session/trust/message 状态，并清除停止态；composer 发送按钮在 running/streaming/waiting approval/waiting question 且存在 active turn 时切为停止按钮，使用 `Square` icon、`button` type 和明确 aria/title。`desktop/src/styles.css` 新增 `.send-button.stop-button` 和 disabled 样式。验证：`npm run build` 通过；fake App Bridge + Playwright smoke 通过，输入 prompt 后按钮切为 `.send-button.stop-button`，点击停止命中 `POST /api/turns/turn_ui_interrupt/interrupt` 恰好 1 次，UI 显示“已中断”，按钮回到 `aria-label="Run prompt"`；smoke 无 console warning/error，fake bridge request count 为 52，最终 fake turn state 为 `interrupted`。残留：backend cancel 仍是 cooperative/in-memory，不是完整 durable job scheduler；Desktop 仍缺 active job list、历史 job 状态页和多 turn 并发控制。
  - 2026-07-02: Async turn cooperative interrupt slice 完成。本轮推进 Rust Agent Runtime / App Bridge 的 interrupt/cancel 语义：async turn 不再只是后台线程跑到底，`POST /api/turns/{turn_id}/interrupt` 会设置进程内 cancel token，provider streaming loop 会协作退出，避免出现“用户已 interrupt 但 turn 继续 completed”的产品错觉；不做跨进程重启恢复、不做完整 durable queue、不改 Desktop UI、不推 GitHub。`runtime/http/src/http_runtime.rs` 新增进程内 turn job registry，记录 `session_id`、`turn_id`、`status`、started/updated/cancel timestamp 和 `Arc<AtomicBool>` cancel token；`start_turn_async_payload` 接受 async turn 时注册 job，后台线程结束时标记 completed/failed/interrupted；新增 `GET /api/turns/{turn_id}`，优先返回 runtime job registry 状态，job 不在内存时回退 session store 的 `run.json` / `summary.json`；`POST /api/turns/{turn_id}/interrupt` 先设置 cancel token，即便 run 目录尚未创建，也能通过 job registry 找到 session；provider SSE stream reader 在每个 SSE JSON frame 前检查 cancel token，命中后 provider loop 转为 `turn/interrupted`，不再追加 `turn/completed`；interrupted finalizer 去重 terminal event；App Bridge protocol manifest 增加 `turn: GET /api/turns/{turn_id}`。`runtime/app-server-client/src/app_bridge_client.rs` 新增 `RemoteRuntimeClient::turn_status(...)`。`runtime/http/tests/http_runtime.rs` 新增 `async_turn_interrupt_cancels_provider_stream_before_completion`。验证：`cargo fmt --all` 通过；新增 interrupt test 通过，slow streaming provider first delta 已进入 App Bridge，interrupt 后 status `interrupted`，events 含 `item/agentMessage/delta` 和 `turn/interrupted` 且不含 `turn/completed`；async streaming 回归 test 通过；`cargo test -p openagent-app-server-client --lib -- --nocapture` 通过；`cargo check -p openagent-http-runtime -p openagent-app-server-client` 通过；`cargo fmt --all -- --check` 通过；`git diff --check` 通过。残留：这是进程内 cooperative cancel，不是完整 durable job scheduler；runtime 重启后无法恢复 job registry，也没有并发 turn quota、持久 worker queue 或 provider HTTP request 主动 abort handle；取消检查发生在 provider SSE frame 到达时，若 provider 长时间不发 frame，后台 reader 可能等到 HTTP read timeout 或下一个 frame 才退出；Desktop UI 还没有专门展示 job status/cancel progress。
  - 2026-07-02: Packaged `.app` real Sub2API streaming smoke slice 完成。本轮推进 Release/Desktop 产品形态战线：真实 macOS `.app` 经 LaunchServices 启动后，通过 bundle 内 Rust App Bridge 调用公网 Sub2API `gpt-5.4-mini`，并在 Desktop 中展示真实 provider streaming + persisted assistant message；不做签名、公证、Windows、auto-update，不推 GitHub。`desktop/scripts/smoke-packaged-app.mjs` 新增 `--workflow=real-streaming`，默认模型锁到 `gpt-5.4-mini`，默认 Base URL `http://47.116.192.3/v1`，API key 从环境或 `.openagent/openagent.env` 读取但只输出 `set(len=...)`；real workflow 先检查 `/api/models?check=true`，再创建真实 session，异步启动 `/turns`，要求 runtime events 含 `item/agentMessage/delta` 和 `turn/completed`，并要求 `/messages` 有持久化 assistant text；marker 只作为 evidence 字段，不再作为真实模型 smoke 的唯一完成条件，避免模型轻微偏离格式误判产品链路失败；`bridgeEvents` 支持 `live_timeout_ms`；截图改为 Swift/CoreGraphics 找 OpenAgent window id，再用 `screencapture -l<window_id>` 截窗口，避免被其他 macOS 前台窗口遮挡。`desktop/package.json` 新增 `smoke:packaged-app:real-streaming`。验证：`node --check scripts/smoke-packaged-app.mjs` 通过；`npm run smoke:packaged-app:real-streaming` 通过，health `ok:true/auth_required:true/service:"openagent-http-runtime"`，runtime 来自 bundle resource；provider health model `gpt-5.4-mini`、model_count `18`、configured_model_available `true`、key 脱敏 `set(len=67)`；real workflow elapsed 约 `2385ms`，persisted assistant text length `57`，runtime events `event_count=15/delta_count=13/completed_count=1/failed_count=0/turn_model=gpt-5.4-mini`；截图 `/tmp/openagent-packaged-real-streaming-smoke.png` 已检查，窗口级截图只包含 OpenAgent `.app`，timeline 显示真实 user prompt 和 assistant marker 输出；`npm run build` 通过；显式 whitespace 检查通过；无残留 OpenAgent/Bridge/smoke 进程，LaunchServices env 已清理。残留：真实 provider smoke 不覆盖 tool approval/diff/checkpoint，fake provider packaged workflow smoke 覆盖该链路；仍缺 durable job scheduler、cancel token、并发 turn 管理、PTY terminal、MCP panel、正式项目选择器、签名/公证/auto-update/Windows packaging。
  - 2026-07-02: Packaged `.app` workflow smoke slice 完成。本轮推进 Rust Desktop 产品闭环：真实 macOS `.app` 经 LaunchServices 启动后完成 `async run -> streaming delta -> tool approval -> tool write -> diff/checkpoint -> rollback`。`desktop/scripts/smoke-packaged-app.mjs` 新增 `--workflow=approval-rollback`，内置 OpenAI Responses-compatible fake streaming provider，创建真实 session，异步启动 turn，等待 approval，调用 `/api/approvals/{request_id}` allow，验证 final assistant text、workspace 文件内容、diff、checkpoint restore 和 `/api/events`；文件校验改为 session workspace 本地文件，避免 `/api/files` root 与 session cwd 不一致误判。`desktop/package.json` 新增 `smoke:packaged-app:workflow`。`desktop/src-tauri/src/lib.rs` diagnostics 增加 `workspace_default_source`，让 `OPENAGENT_WORKSPACE` 注入的 packaged smoke workspace 成为 Desktop 默认 project。`desktop/src/App.tsx` 收到 sessionChanged 且无 active session 时自动选择被触达 session 并刷新 messages/trust；timeline/trust history 现在按 approval/question request 折叠旧 pending part，只显示最新 resolved 状态。验证：`node --check scripts/smoke-packaged-app.mjs` 通过；`npm run build` 通过；`npm run tauri -- build --bundles app` 通过；`npm run smoke:packaged-app:workflow` 通过，health `ok:true/auth_required:true/service:"openagent-http-runtime"`，runtime 来自 bundle resource，event methods 覆盖 `turn/started`、`item/agentMessage/delta`、`item/toolCall/started`、`turn/approval_requested`、`turn/approval_resolved`、`item/toolCall/completed`、`patch/detected`、`checkpoint/restored`，fake provider `/v1/responses` 请求数 2，rollback 后 `workflow.txt` 不存在；截图 `/tmp/openagent-packaged-workflow-smoke.png` 已检查，真实 `.app` 窗口显示 approval 为 `ALLOWED`、patch card 和 restored checkpoint card；`git diff --check` 通过；无残留 OpenAgent/Bridge/smoke 进程，LaunchServices 注入 env 已清理。残留：仍是 fake provider deterministic packaged smoke，后续需要真实 Sub2API packaged app 长会话 smoke；async turn 还不是 durable job scheduler；Desktop 还缺项目选择器、PTY terminal、MCP panel、签名/公证/auto-update/Windows packaging。
  - 2026-07-02: Async turn + long-lived SSE Desktop slice 完成。本轮推进 Rust App Bridge / Desktop 第一条闭环中的真实 Agent run 语义：`POST /api/sessions/{id}/turns` 不再只能 blocking 等完整 provider/tool loop 完成，Desktop 提交任务后可以快速返回 UI，由 `/api/events` SSE 持续驱动 streaming、tool、approval、diff/checkpoint 后续刷新；不改 Python、不推 GitHub、不做 interrupt/cancel 深化、不重构 CLI/TUI，旧同步 `/turns` 默认兼容。`runtime/http/src/http_runtime.rs` 新增 async route 语义：body/query 中 `async=true`/`background=true`/`run_async=true` 时返回 HTTP `202`、`status:"running"`、`accepted:true`、`turn_id`，后台线程复用同一 `start_turn_payload_inner` 执行 Rust provider/tool loop，事件仍写入同一 App Bridge JSONL/SSE channel；默认同步 `200` 行为保留；protocol manifest 的 `turns` endpoint 描述补充 async 语义。`runtime/app-server-client/src/app_bridge_client.rs` 新增 `RemoteRuntimeClient::start_turn_async(...)`。`desktop/src/App.tsx` 的 composer submit 改为发送 `async:true`，只等待 accepted response，不再 await 完整 provider completion；后续由 SSE + scheduled refresh 收敛 messages/trust/diff/checkpoint；`/api/events` live timeout 从 5s 提升到 300s，更接近 long-lived subscription。`runtime/http/tests/http_runtime.rs` 新增 `async_turn_returns_accepted_before_provider_completion_and_streams`。验证：`cargo fmt --all -- --check` 通过；新增 async runtime test 通过，HTTP response 为 `202` 且在 fake provider 1.5s 延迟完成前返回，随后 turn SSE 收到 `turn/started`、`item/agentMessage/delta`、`turn/completed`，final answer 为 `streamed answer`；`app_bridge_protocol_contract_and_client_live_subscription` 通过；`cargo test -p openagent-app-server-client --lib -- --nocapture` 通过；旧同步 `global_sse_live_tails_provider_stream_delta_before_completion` 回归通过；`cargo check -p openagent-http-runtime -p openagent-app-server-client` 通过；`npm run build` 通过；`npm run smoke:streaming` 通过，provider request body `stream=true`，首个 delta 比 completion 早约 `2500ms`，raw delta 未泄漏，persisted assistant text 为 `streamed answer`；`openagent_process_count=0`。残留：后台 async thread 还不是完整 durable job scheduler，没有进程重启恢复、并发 turn 限制或 cancel token 深化；Desktop 仍是 fetch+SSE loop，不是专门 turn-scoped subscription UI；packaged `.app` 长会话 async smoke 还没单独覆盖。
  - 2026-07-02: LaunchServices packaged GUI smoke slice 完成。本轮推进 Release/Desktop 产品形态战线：验证 OpenAgent 能通过 macOS LaunchServices/Finder 风格启动真实 `.app` 窗口，并由 Tauri command 拉起 bundle 内 Rust App Bridge；不做签名/notarization、Windows、auto-update，不推 GitHub。`desktop/scripts/smoke-packaged-app.mjs` 新增 `--launch=launchservices`，通过 `open -n OpenAgent.app` 启动；LaunchServices 路径用 `launchctl setenv` 临时注入 workspace/session/token/port/provider env 和 strict bundle runtime 开关，finally 恢复/清理，token/key 不输出；新增 `--screenshot` 稳定态截图，并加按 port 清理 `openagent-http-runtime` 兜底，修复首轮发现的 app 退出后 bridge 子进程残留。`desktop/package.json` 新增 `smoke:packaged-app:launchservices`。`desktop/src/App.tsx` 在 Tauri runtime 下只有 managed bridge running 后才启动 App Bridge API refresh/SSE/session trust refresh，避免旧 localStorage bridge/token 启动早期产生 `Load failed`；WebKit `Load failed` 归入启动期可忽略 fetch 错误，provider catalog 探测失败降级到 provider 状态而非全局 composer 错误。验证：`npm run tauri -- build --bundles app` 通过；`npm run smoke:packaged-app:launchservices` 通过，health `ok:true/auth_required:true/service:"openagent-http-runtime"`，runtime 来自 `OpenAgent.app/Contents/Resources/openagent-http-runtime`，截图 `/tmp/openagent-launchservices-smoke.png` 已检查，macOS 菜单栏显示 OpenAgent、独立 `.app` 窗口展示 Codex-like shell、sidebar project online、composer 无 `Load failed`；`npm run smoke:packaged-app` 回归通过；`node --check scripts/smoke-packaged-app.mjs` 通过；whitespace checks 无错误；`openagent_process_count=0`，LaunchServices 注入 env 无残留。残留：还没做 code signing/notarization/auto-update/crash logs/Windows packaging；还没做长会话 timeline/tool cards/approval dock 的 packaged app 截图。
  - 2026-07-02: Packaged runtime resource bundle slice 完成。本轮推进 Release/Desktop 产品形态战线：packaged `.app` 不再依赖 repo `target/debug` 或 `OPENAGENT_HTTP_RUNTIME` 环境变量才能拉起 Rust App Bridge；不做签名/notarization、不做 Windows、不推 GitHub。`desktop/src-tauri/tauri.conf.json` 的 `beforeBuildCommand` 改为 `npm run build && cargo build -p openagent-http-runtime --release`，并在 `bundle.resources` 新增 `../../target/release/openagent-http-runtime -> openagent-http-runtime`，打包进 `.app/Contents/Resources/openagent-http-runtime`。`desktop/src-tauri/src/lib.rs` 保持 env + bundle lookup 优先，并新增 `OPENAGENT_DESKTOP_DISABLE_DEV_RUNTIME_FALLBACK`；开启后不再回退到 repo `target/{debug,release}` 或 PATH。`desktop/scripts/smoke-packaged-app.mjs` strict smoke 不再设置 `OPENAGENT_HTTP_RUNTIME`，设置 `OPENAGENT_DESKTOP_DISABLE_DEV_RUNTIME_FALLBACK=1`，同时清掉 env runtime，断言 `Contents/Resources/openagent-http-runtime` 存在并输出 bundled bridge path。验证：`npm run tauri -- build --bundles app` 成功，日志显示 release 版 `openagent-http-runtime` 被构建后再 bundle；bundle 内存在 `desktop/src-tauri/target/release/bundle/macos/OpenAgent.app/Contents/Resources/openagent-http-runtime`，权限 `-rwxr-xr-x`，大小约 `8.98MB`；`npm run smoke:packaged-app` 通过，strict 条件 no `OPENAGENT_HTTP_RUNTIME` + dev fallback disabled，health `ok:true/auth_required:true/service:"openagent-http-runtime"`，token redacted `set(len=75)`；`cargo test --manifest-path desktop/src-tauri/Cargo.toml managed_bridge_starts_restarts_and_stops_runtime --lib -- --nocapture` 通过；`npm run build` 通过；`cargo fmt --manifest-path desktop/src-tauri/Cargo.toml -- --check` 通过；tracked/no-index whitespace checks 无错误；无残留 `openagent-desktop`、`openagent-http-runtime`、`smoke-packaged-app` 进程。残留：仍是直接启动 `.app/Contents/MacOS/*`，不是 Finder/LaunchServices 双击 smoke，也没有窗口截图；还没做 code signing/notarization/auto-update/crash logs/Windows packaging。
  - 2026-07-02: Packaged Tauri App Bridge smoke slice 完成。本轮推进 Release/Desktop 产品形态战线，验证 OpenAgent 不只是 Vite/web preview 能跑，而是 macOS packaged `.app` 能启动，并通过 Tauri command 拉起 Rust App Bridge；不做签名/notarization、不做 Windows、不推 GitHub。`desktop/src-tauri/src/lib.rs` 新增 `OPENAGENT_BRIDGE_PORT` 支持，`default_bridge_url()`、`stopped_bridge_status()`、managed bridge start 默认端口都走同一个 `default_bridge_port()`；默认仍是 `8787`，但 smoke/测试可用随机端口，避免撞本机已有 Bridge。新增 `desktop/scripts/smoke-packaged-app.mjs`：启动真实 bundle `/desktop/src-tauri/target/release/bundle/macos/OpenAgent.app/Contents/MacOS/*`，使用临时 HOME/workspace/session root/token path 和随机 `OPENAGENT_BRIDGE_PORT`，等待 Tauri Desktop 生成 `oa_desktop_*` token，再验证 Rust App Bridge `/api/health` 返回 `ok:true` 且 auth enabled；token 只输出 `set(len=...)`。`desktop/package.json` 新增 `npm run smoke:packaged-app`。验证：`npm run tauri -- build --bundles app` 成功产出 `desktop/src-tauri/target/release/bundle/macos/OpenAgent.app`；`npm run smoke:packaged-app` 通过，app process started，random bridge URL example `http://127.0.0.1:55054`，health 返回 `ok:true/auth_required:true/service:"openagent-http-runtime"/ui_enabled:false`，token redacted `set(len=75)`；`cargo test --manifest-path desktop/src-tauri/Cargo.toml managed_bridge_starts_restarts_and_stops_runtime --lib -- --nocapture` 通过；`npm run build` 通过；`cargo fmt --manifest-path desktop/src-tauri/Cargo.toml -- --check` 通过；tracked/no-index whitespace checks 无错误；确认无残留 `openagent-desktop`、`openagent-http-runtime`、`smoke-packaged-app` 进程。残留：这是直接启动 `.app/Contents/MacOS/*` 的 packaged smoke，不是 Finder/LaunchServices 双击路径，也没有做窗口截图；Bridge binary 仍依赖 repo `target/debug/openagent-http-runtime` 或显式 `OPENAGENT_HTTP_RUNTIME`，还没有作为 app bundle resource 打进去；尚未做 code signing/notarization/auto-update/crash logs/Windows packaging。
  - 2026-07-01: Desktop real Sub2API streaming smoke slice 完成。本轮推进第一条产品闭环里的真实模型流式输出：验证 `gpt-5.4-mini` 通过 Rust App Bridge 到 Desktop UI，而不是只走 fake provider；不改 Python、不提交、不推远端。`desktop/scripts/smoke-real-streaming.mjs` 的真实模式现在强制在 Desktop model picker 选择 `gpt-5.4-mini`，避免本地 env 默认 `gpt-5.5` 污染验证；真实 prompt 调整为 12 行 marker 输出，失败诊断新增脱敏 runtime event summary、persisted assistant summary、page streaming summary，API key 只输出 `set(len=67)`；成功路径断言 runtime `turn_model == gpt-5.4-mini`、`delta_count > 0`、`completed_count > 0`。`desktop/src/App.tsx` 的 SSE reader 对 `item/agentMessage/delta` 逐帧让步，让批量到达的真实 SSE frame 也能被浏览器绘制成 streaming draft；同步 `/turns` 返回中包含 delta 时，先绘制短暂 draft，再刷新持久化 transcript，避免真实 provider 过快完成导致 UI 看不到流式态。验证：`npm run smoke:streaming:sub2api` 通过，真实 provider config 为 `http://47.116.192.3/v1` + `gpt-5.4-mini` + `responses`，key 已脱敏；runtime events `event_count=43/delta_count=41/completed_count=1/failed_count=0/turn_model=gpt-5.4-mini`；Desktop page final state raw delta rows 未泄漏、Vite overlay false、final assistant text rendered、`OA_REAL_STREAM_END` 可见；persisted assistant text length `186`，包含 begin/end markers。Regression：`npm run smoke:streaming` 通过，fake provider request body `stream=true`，首个 delta 比 completion 早约 `2504ms`；`cargo test -p openagent-http-runtime provider_sse_stream_stops_on_responses_completed_without_done --lib -- --nocapture` 通过；`npm run build` 于 `desktop/` 通过；tracked/no-index whitespace checks 通过。残留：仍是 Vite/web preview Desktop smoke，不是 packaged Tauri `.app` 真窗口；`/turns` endpoint 仍是同步返回，更理想产品语义是后续做真正 async run + long-lived event subscription。
  - 2026-07-01: Desktop Codex reference UI polish slice 完成。按 William 截图反馈“UI 太丑，模仿 Codex”，本轮只改 Desktop React/CSS 视觉和一个 streaming draft 收口，不改 Rust runtime/CLI/TUI 语义、不提交、不推远端。`desktop/src/App.tsx` 将顶部项目 icon 从 database 换成更接近 Codex 的 panel/icon，项目列表 icon 换成 folder；顶部右侧状态从调试感较强的 provider/plug 文案状态改成轻量 icon buttons，状态保留在 tooltip；`activeStreamingDraft` 增加 persisted assistant message 收口，完成后的 draft 在持久化 assistant message 出现后不再重复占位。`desktop/src/styles.css` 重调 Codex-like token：macOS 灰色 sidebar、白色阅读区、轻 topbar、居中 timeline、底部悬浮 composer；收敛 sidebar 行高、选中态、图标线宽、顶部按钮、composer 阴影和空状态比例；消息流保持 assistant 文档流、user 右侧深色气泡、tool/live event 弱化展示。验证：`npm run build` 于 `desktop/` 通过；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css desktop/package.json runtime/http/src/http_runtime.rs progress.md .goal/state.md` 通过；参考 Codex 截图已用 `view_image` 检查；Vite dev `http://127.0.0.1:5651/` 桌面 `2048x1240` 截图 `/tmp/openagent-ui-after.png` 已用 `view_image` 检查；移动 `390x844` 截图 `/tmp/openagent-ui-mobile.png` 已用 `view_image` 检查且无横向 overflow。残留：本轮是 Vite/web preview 视觉验证，未做 packaged Tauri `.app` 真窗口截图；当前截图为空项目/空会话态，真实长会话内容态还需要后续用 persisted messages 再按 Codex 截图细调 typography 和事件行节奏。
  - 2026-07-01: Desktop real App Bridge streaming smoke slice 完成。本轮把上一段 Desktop delta draft 从前端 mock SSE 验证升级为真实 Rust App Bridge + Desktop 的可重复垂直 smoke，不改 Rust runtime 语义、不碰 Python、不提交、不推远端。新增 `desktop/scripts/smoke-real-streaming.mjs`：脚本启动本地 OpenAI-compatible fake streaming provider（`/v1/models` + `/v1/responses` SSE）、当前 Rust `openagent-http-runtime`（`OPENAI_WIRE_API=responses`、`OPENAGENT_PROVIDER_STREAM=1`）、Vite Desktop，预置 App Bridge URL/token/project 后通过 Playwright 提交 prompt；断言 provider completion 前 Desktop 已出现 `[data-testid="streaming-assistant-draft"]`，completion 后 draft 消失、最终 `streamed answer` 渲染、raw `agentMessage delta` rows 不泄漏到 timeline、Vite overlay/console/page errors 为空。`desktop/package.json` 新增 `npm run smoke:streaming`。验证：`npm run smoke:streaming` 通过，fake provider 收到 `/v1/responses` 且 request body `stream=true`，首个 delta 比 completion 早约 `2501ms`；`npm run build` 于 `desktop/` 通过；untracked Desktop package/script/App/CSS 通过 `git diff --no-index --check`。残留：这是 fake provider + 真实 App Bridge/Desktop 的 deterministic smoke，不消耗真实 Sub2API；真实 Sub2API streaming 和 packaged Tauri `.app` GUI smoke 仍建议后续单独做。
  - 2026-07-01: Desktop Codex UI simplification follow-up 完成。根据 William 最新反馈“UI 太丑，模仿 Codex”，本轮继续收敛 Desktop 视觉，不改 Rust runtime/CLI/TUI 语义、不提交、不推远端。`desktop/src/App.tsx` 新增 `showComposerContext`，只有有项目/会话/运行态/streaming draft/approval dock 时才显示 composer 上方状态条；空状态不再强行显示“新任务”step pill/context bar；textarea 起始行数从 3 行降到 2 行。`desktop/src/styles.css` 轻量化 rail/topbar/timeline/composer tokens，新增 bare composer 状态，空项目/空会话时保留更接近 Codex 的干净底部悬浮输入框，移动端保持单栏不溢出。验证：`npm run build` 于 `desktop/` 通过；Browser/IAB 打开 `http://127.0.0.1:5174/`，桌面 1280x720 截图检查 rail 306px、topbar 52px、bare composer 850x124、context bar count 0、console warnings/errors 为空；临时 390x740 移动视口检查 rail hidden、composer 358x122、`scrollWidth == 390`、console warnings/errors 为空，随后恢复默认视口；untracked Desktop TS/CSS 通过 `git diff --no-index --check`。残留：未做 packaged Tauri `.app` 截图；真实长会话内容态还需继续按 Codex 截图微调 timeline typography。
  - 2026-07-01: Desktop streaming delta draft UI slice 完成。本轮把 Desktop 对 `item/agentMessage/delta` 的展示从 raw protocol event 行升级为 Codex-like assistant streaming draft：`desktop/src/App.tsx` 新增 `StreamingDraft`、`activeStreamingDraftFromEvents`，根据当前 active session 的 SSE events 聚合最新未完成 turn 的 delta；timeline 渲染时过滤 raw `item/agentMessage/delta` live event，改为一条 `role-assistant streaming-draft` 消息行，并保留非 delta 的 turn/tool/checkpoint live logs。`desktop/src/styles.css` 增加轻量 streaming caret，形成“assistant 正在生成”的视觉状态。验证：`npm run build` 于 `desktop/` 通过；Playwright + system Chrome mock App Bridge `/api/events`，先返回 `turn/started` + 第一段 `item/agentMessage/delta`，断言 `[data-testid="streaming-assistant-draft"]` 显示 `streamed `，且 `.live-event-row` 中没有 `agentMessage delta` raw 行；随后 mock completed 状态和 persisted `/messages`，断言 final assistant message 显示 `streamed answer`，draft 消失，consoleIssues/pageErrors 均为空；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css .goal/state.md progress.md` 通过。临时 QA 截图已清理。残留：这是 Vite/web preview 的 mock SSE 烟测，未做真实 Sub2API streaming Desktop E2E；CLI/TUI 的 streaming 表现未改；finished 后的 message persistence 仍依赖 `/messages` refresh。
  - 2026-07-01: App Bridge provider loop MCP tool execution slice 完成。本轮把上一段 MCP discovery 从“能显示 tool_count”推进到真实 Rust HTTP runtime/provider loop 执行路径：HTTP runtime 启动 provider loop 时会加载 `OPENAGENT_MCP_CONFIG`/`--mcp-config`，对 enabled MCP server 执行 `tools/list` discovery，把 MCP tool definition 注册进 `Toolkit`，让 provider 请求里的 `tools` 包含 `mcp_tool_*` 动态工具；provider 发回 MCP function_call 后，runtime 走同一 permission gate，执行 `mcp_json_rpc(..., "tools/call", ...)`，把 normalize 后的 MCP result 写成 `ToolResult`，再作为 `function_call_output` 送回 provider 继续生成最终答案。修复了半截改动导致 `openagent-http-runtime` 编译失败的 `config` 参数传递问题；integration test 环境也清理 `OPENAGENT_MCP_CONFIG`，避免本机配置污染。新增测试 `remote_runtime_client_provider_loop_executes_mcp_tool`：fake OpenAI-compatible responses provider 首轮请求断言能看到 `mcp_tool_remote_tools_echo`，随后返回 MCP tool call；mock MCP server 断言收到 `tools/list` 与 `tools/call`，返回 `mcp echo: from-provider`；第二轮 provider 请求断言包含 `function_call_output` 与 MCP 输出，最终 turn 返回 `mcp final answer`。验证：`cargo fmt --all` 通过；`cargo test -p openagent-http-runtime remote_runtime_client_provider_loop_executes_mcp_tool --test http_runtime -- --nocapture` 通过；`cargo test -p openagent-http-runtime app_bridge_mcp_refresh_discovers_tools_without_leaking_endpoint_secret --lib -- --nocapture` 通过；`cargo check -p openagent-http-runtime -p openagent-cli -p openagent-mcp` 通过；`git diff --check -- runtime/http/src/http_runtime.rs runtime/http/tests/http_runtime.rs progress.md .goal/state.md` 通过。残留：MCP installer/auth UI、Desktop MCP tool invocation UI、MCP approval resume 的 dedicated E2E、真实 Arbor MCP smoke 还没做。
  - 2026-07-01: App Bridge MCP refresh discovery + shared MCP runtime slice 完成。本轮把 `/api/mcp` 从纯配置/status 快照推进到显式 refresh discovery：默认 `GET /api/mcp` 仍保持安全低副作用摘要，`GET /api/mcp?refresh=true` / `?check=true` 会复用 `openagent-mcp` 的 JSON-RPC discovery 对 enabled server 执行 `tools/list`，把 discovered tools 写入 `RemoteMcpManager` snapshot，并在 payload 中显示 `tool_count`、`selected_transport`、server `connected/error` 状态和 tool descriptors；错误路径会脱敏 authorization/api_key/password/secret/token 等敏感信息。CLI `prompt/mcp_runtime` 删除重复 discovery/stdin/http JSON-RPC 实现，改为复用 `openagent-mcp::{discover_mcp_server_tools,mcp_json_rpc}`，保留 `execute_mcp_tool` 作为 Agent Loop MCP tool call 执行入口。Desktop refresh 改为拉取 `/api/mcp?refresh=true`，右侧 MCP card 可显示真实 discovered tool_count。顺手清理 CLI 旧 provider SSE dead parser 和 MCP unused imports。验证：`cargo fmt --all` 通过；`cargo test -p openagent-http-runtime app_bridge_mcp_refresh_discovers_tools_without_leaking_endpoint_secret --lib -- --nocapture` 通过，mock MCP server 断言 `tools/list` 请求并验证 payload `tool_count=1/status=connected/selected_transport=http` 且不泄露 `token=`；`cargo test -p openagent-http-runtime app_bridge_mcp_status_sanitizes_config --lib -- --nocapture` 通过；`cargo test -p openagent-mcp --lib --tests -- --nocapture` 通过；`cargo check -p openagent-http-runtime -p openagent-cli -p openagent-mcp` 通过且无 warnings；`npm run build` 于 `desktop/` 通过；`cargo fmt --all -- --check`、`git diff --check`、untracked Desktop TS/CSS `git diff --no-index --check` 均通过。残留：MCP discovery 现在能显示真实 tool_count，CLI Agent Loop 已可通过共享 runtime 执行 MCP tool call，但 App Bridge provider loop/HTTP runtime 还没有把 MCP tools 并入 Rust Agent Toolkit；Desktop 只展示 discovery，不提供 MCP tool invocation UI。
  - 2026-07-01: App Bridge MCP status + Desktop inspector slice 完成。本轮补齐长期目标中 Rust App Bridge 的 `/api/mcp` 可见产品面，不改 MCP tool execution/Agent Loop、不推远端。`Cargo.toml` 新增 workspace dependency `openagent-mcp`，`cli/Cargo.toml` 统一改用 workspace dependency，`runtime/http/Cargo.toml` 接入 `openagent-mcp`；`runtime/http/src/http_runtime.rs` 新增 `HttpRuntimeConfig::mcp_config`、`--mcp-config <json-or-path>`、protocol manifest `mcp: GET /api/mcp`、router `GET /api/mcp` 和 `mcp_payload`。payload 只返回 source/configured/enabled/server_count/tool_count/refresh_ttl/server summaries，暴露 command 首项、args/env/header 数量、remote_url_configured，不泄露 env/header/url query/command args 里的 secret。`desktop/src/App.tsx` 新增 MCP payload/server 类型、MCP state、refresh 拉取 `/api/mcp`（失败降级不阻断其他刷新）和 Overview MCP inspector card；`desktop/src/styles.css` 调整 file-row 布局，避免 MCP server 行 title/subtitle 挤在一起。验证：`cargo fmt --all -- --check` 通过；`cargo test -p openagent-http-runtime app_bridge_mcp_status_sanitizes_config --lib -- --nocapture` 通过，覆盖 protocol manifest、router status 和 secret marker 不泄露；`cargo check -p openagent-http-runtime -p openagent-cli` 通过；`npm run build` 于 `desktop/` 通过；临时 App Bridge `127.0.0.1:18800` + MCP config smoke 返回 `configured=true/server_count=2/source=config` 且 payload grep 无 `smoke-secret|ARBOR_TOKEN|token=`；Desktop Playwright smoke 打开 inspector，`[data-testid="mcp-card"]` 显示 `arbor/docs`、`Servers 2`、`Tools 0`，card bbox 在 viewport 内，无横向 overflow，card text 未泄露 secret；tracked files `git diff --check` 和 untracked Desktop TS/CSS `git diff --no-index --check` 均通过；临时 App Bridge、MCP config、截图已清理。残留：这是配置/status 面，未执行 MCP discovery/tools/list，`tool_count` 在未 refresh 前仍为 0；MCP tools 还未真正并入 Agent Loop。
  - 2026-07-01: Desktop activity-backed goal strip slice 完成。本轮把上一段 Codex-like 底部 context/goal strip 从静态 project/path 状态升级为真实 session activity 视图，不改 Rust runtime/CLI/TUI 语义、不提交、不推远端。`desktop/src/App.tsx` 新增 `compactText`/`formatElapsed` helper、30s 前端 elapsed tick、`latestUserActivity`/`latestTurnStartedAtMs` 派生逻辑；composer context bar 现在显示“进行中的目标/等待处理/最近任务/当前会话/新任务”、最近 user prompt 摘要、elapsed + phase，并加 `data-testid` 便于后续第一闭环 smoke 断言。验证：`npm run build` 于 `desktop/` 通过；启动临时 Rust App Bridge `127.0.0.1:18799`，创建真实 session 并发起 turn，`GET /messages` 返回 persisted user text `Activity strip should show this real persisted prompt`；Vite dev `127.0.0.1:5173` + system Chrome Playwright 桌面 smoke 断言 `[data-testid="composer-activity-detail"]` 包含该真实 prompt，context bar 文本为 `最近任务 / Activity strip should show this real persisted prompt / 1m · 可以继续发这段`，无横向 overflow；移动 smoke 断言 hidden textContent 包含该真实 prompt，innerText 显示 `最近任务 / 2m · 可以继续发这段`，无横向 overflow；tracked progress 文件 `git diff --check` 通过，untracked Desktop TS/CSS 用 `git diff --no-index --check /dev/null <file>` 检查通过；临时 App Bridge 已停止。残留：移动端为节省空间隐藏长 prompt；真实 goal step count/计划阶段仍需后续从 runtime/goal metadata 接入。
  - 2026-07-01: Desktop Codex-like composer/context polish slice 完成。根据 William 最新截图反馈“这个 UI 做的太丑，模仿 Codex”，本轮只改 Desktop 前端视觉和轻量 app shell 结构，不改 Rust runtime/CLI/TUI 语义、不提交、不推远端。`desktop/src/App.tsx` 在 composer 上方新增 Codex-like 当前上下文状态条，展示当前 session/project/path/stream state，并保留打开详情入口；`desktop/src/styles.css` 进一步收敛 Codex 参考图的浅灰 sidebar、白色阅读画布、轻 topbar、居中 timeline、底部浮动 composer、状态胶囊和移动端单栏，降低 dashboard/card 感和过重阴影。验证：`npm run build` 于 `desktop/` 通过；Vite dev `http://127.0.0.1:5173/` + system Chrome Playwright 截图验证桌面 `2048x1255` 与移动 `390x844`，两者无横向 overflow，composer/context bar 在 viewport 内，sidebar 桌面可见、移动端隐藏；参考 Codex 截图和渲染截图已用 `view_image` 人工对比；tracked progress 文件 `git diff --check` 通过，untracked `desktop/src/App.tsx`/`desktop/src/styles.css` 用 `git diff --no-index --check /dev/null <file>` 检查通过；临时 QA 截图和 npm 生成的临时 lockfile 已清理。残留：本轮仍是 Vite/web preview 视觉验证，未做 packaged Tauri GUI click smoke；真实会话内容/goal 文案需要后续接入 runtime 数据后再按 Codex 截图精修。
  - 2026-07-01: Desktop startup/restored project -> managed App Bridge auto-sync slice 完成。修复上一轮 workspace sync 的启动恢复边界：`desktop/src/App.tsx` 新增 `managedBridgeAutoSyncKey`，Desktop 在 Tauri runtime、auth ready、active project/default workspace 已知且 bridge 不忙时，会自动检测 managed bridge 是否 stopped 或 workspace mismatch，并触发 `app_bridge_start`/`app_bridge_restart` 到当前项目；当 bridge 已对齐时同步 `bridgeUrl` 并标记 ready key；命令失败时清空 auto-sync key，允许后续状态变化或用户选择重试。`createSession` 自身也增加 `bridgeSwitchInProgress` guard，避免绕过按钮 disabled 时创建旧 workspace session。验证：`npm run build` 于 `desktop/` 通过；内置 Browser 打开 `http://127.0.0.1:5173/` smoke 通过，web preview 下 Tauri-only effect 未误触发，`.app-shell`/`.composer` 存在，send/new-session 未误禁用，无横向 overflow，console warnings/errors 为空；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css .goal/state.md progress.md` 通过。残留：仍未做 packaged Tauri GUI click smoke；真实 start/restart 子进程行为依赖前序 Tauri command 测试覆盖。
  - 2026-07-01: Desktop project selection / managed App Bridge workspace sync slice 完成。本轮把 Desktop 的“选择项目”从纯 React 本地状态推进成 Rust product path 入口：`desktop/src/App.tsx` 新增 `bridgeSwitchInProgress`、`managedBridgeWorkspaceMismatch` 和项目 bridge 状态标签；`managedBridgeStartOptions` 支持 workspace override；抽出 `runManagedBridgeCommand("start"|"restart", workspaceOverride)`，项目选择/注册时会根据当前 managed bridge 状态自动 `app_bridge_start` 或 `app_bridge_restart` 到目标 workspace，并清空旧 session/messages/diff/checkpoint/file/git 上下文，避免 UI 继续显示旧项目数据；切换期间禁用“新对话”、项目选择和发送按钮，`submitPrompt` 也会阻止 prompt 发到旧 workspace。验证：`npm run build` 于 `desktop/` 通过；内置 Browser 打开 `http://127.0.0.1:5173/` smoke 通过，页面 title `OpenAgent Desktop`，`.app-shell` 和 `.composer` 存在，composer/new-session/send 各 1 个，web preview 下未误禁用，`scrollWidth <= innerWidth`，console warnings/errors 为空；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css .goal/state.md progress.md` 通过。残留：这是 Vite/web preview + TS build 验证，没有做真实 packaged Tauri click smoke；自动 start/restart 依赖 Tauri command 层，之前已有真实子进程 command 测试覆盖核心行为。
  - 2026-07-01: CLI/TUI remote attach live SSE client migration slice 完成。延续上一轮 `runtime/app-server-client` 增量 SSE callback API，本轮把 CLI/TUI remote attach 的事件消费后端从手写 HTTP/SSE 批量解析迁到统一 `RemoteRuntimeClient`。`cli/src/remote.rs` 新增 `app_bridge_client(server_url, auth)` helper；`remote_turn_events` 改用 `RemoteRuntimeClient::turn_events_live_stream`，非 TTY attach 在等待 turn 事件时也走 App Bridge client live stream；`RemoteTerminalHandler::poll_app_events` 改用 `RemoteRuntimeClient::global_events_live_stream`，请求带 `Accept: text/event-stream` 和 `live_timeout_ms`，默认 poll timeout 250ms，可用 `OPENAGENT_ATTACH_POLL_MS` 调整；global cursor 更新改为优先 `global_sequence`，dedupe key 优先 `event_id`，避免 global `/api/events?last_event_id=` 在多会话场景错过/重复事件。新增单测 `remote_terminal_poll_app_events_uses_app_bridge_live_stream`：mock server 断言请求命中 `/api/events?last_event_id=0&live_timeout_ms=250`、带 Bearer auth 和 SSE Accept，返回 `sequence=1/global_sequence=7` 的 delta，handler 必须返回该事件并把 `last_global_event_id` 更新为 7。验证：`cargo fmt --all` 通过；`cargo test -p openagent-cli remote_terminal_poll_app_events_uses_app_bridge_live_stream --lib -- --nocapture` 通过；`cargo test -p openagent-cli remote_terminal_command_runs_through_app_bridge_client --lib -- --nocapture` 通过；`cargo test -p openagent-app-server-client live_sse_stream_callback_receives_event_before_response_finishes --lib -- --nocapture` 通过；`cargo check -p openagent-cli -p openagent-app-server-client` 通过；`cargo fmt --all -- --check` 和 `git diff --check` 通过。残留：TUI trait 仍是同步 `poll_app_events -> Vec<Value>`，所以这还不是后台线程推送式实时绘制；Desktop TS reader 尚未切到 Rust client；旧 HTTP helper 仍存在于其他 remote routes。
  - 2026-07-01: App Bridge client incremental live SSE stream slice 完成。本轮先审计 `runtime/http` provider streaming，确认 Rust runtime 已能把 OpenAI-compatible streaming provider 的文本增量转成 `item/agentMessage/delta`，且已有 HTTP runtime 测试证明 delta 早于 `turn/completed` 被 live SSE 看到；因此本 slice 改为补齐统一 client 消费层，而不是重复实现 provider streaming。`runtime/app-server-client/src/app_bridge_client.rs` 新增 `RemoteRuntimeClient::global_events_live_stream`、`turn_events_live_stream`、`sse_events_live_stream`，保留旧 `global_events_live`/`turn_events_live` 批量返回接口；内部抽出 `send_with_options`，让 streaming API 直接读取 `reqwest::blocking::Response` 的 body；新增 `read_sse_response_stream` 增量解析 SSE frame，并在每个 frame 到达时调用 callback。新增单测 `live_sse_stream_callback_receives_event_before_response_finishes`：mock SSE server flush 第一条 `item/agentMessage/delta` 后延迟 700ms 才发 `turn/completed`，client callback 必须在 400ms 内收到第一条事件，证明不再等 HTTP body 完结才返回。验证：`cargo fmt --all` 通过；`cargo test -p openagent-app-server-client live_sse_stream_callback_receives_event_before_response_finishes --lib -- --nocapture` 通过；`cargo test -p openagent-app-server-client app_bridge_client_matches_legacy_oracle --test remote_runtime -- --nocapture` 通过；`cargo test -p openagent-http-runtime global_sse_live_tails_provider_stream_delta_before_completion --test http_runtime -- --nocapture` 通过；`cargo check -p openagent-app-server-client` 通过；`cargo fmt --all -- --check` 和 `git diff --check` 通过。残留：CLI/TUI 还没有切换到 callback live stream API；Desktop 当前仍用 TypeScript fetch reader；App Bridge client stream reader 不处理 provider `[DONE]`，但 App Bridge `/api/events` 不发送 `[DONE]`。
  - 2026-07-01: Desktop Codex reference UI polish follow-up 完成。根据 William 给的 Codex 截图，本轮只改 Desktop 前端壳和视觉细节，不改 Rust runtime/CLI/TUI 语义、不提交、不推远端。`desktop/src/App.tsx` 新增 composer 顶部状态胶囊、改用 Codex-like `+` attach 与圆形上箭头发送，并让空 timeline 带 `empty` 状态；自动 background refresh/trust 初始化遇到 bridge 未启动的 `Failed to fetch` 时不再把 raw 错误显示在主输入区，用户主动操作错误仍保留。`desktop/src/styles.css` 收敛为更贴近 Codex 截图的浅灰 sidebar + 白色主画布 + 轻 topbar + 居中 conversation + 底部浮动 composer：降低 dashboard/card 感，assistant 正文更像阅读流，user 消息右侧气泡，tool/part 状态降噪，空态不显示进度虚线，移动端保持单栏无横向溢出。验证：`npm run build` 于 `desktop/` 通过；Vite dev `http://127.0.0.1:5173/` + system Chrome Playwright 截图验证桌面 `1600x1000` 与移动 `390x844`，两者 `scrollWidth == innerWidth`，composer 在 viewport 内，sidebar 桌面可见、移动端隐藏；参考图和渲染截图已用 `view_image` 人工对比；临时 QA 截图已清理。残留：本轮是 Vite/web preview 视觉验证，未做 packaged Tauri GUI click smoke；App Bridge 未启动时浏览器 console 仍会有预期 `ERR_CONNECTION_REFUSED` 网络日志，但主 UI 不再显示 raw `Failed to fetch`。
  - 2026-07-01: TUI remote terminal command slice 完成。延续上一轮 `RemoteRuntimeClient::terminal_run` 和 `openagent terminal`，把同一 App Bridge terminal runner 接入 remote attach/TUI command backend。`cli/src/remote.rs` 新增共享 `remote_terminal_run_text`/`terminal_payload_text`/`slash_terminal_command` helper，所有请求继续通过 `openagent-app-server-client::RemoteRuntimeClient`；interactive remote attach 命令列表新增 `/terminal <command>`，真实 terminal attach loop 支持 `/terminal`/`/term`；`RemoteTerminalHandler::handle_command` 同样支持 `/terminal <command>`，返回 `terminal` timeline lines；未知命令帮助也显示 `/terminal <command>`。新增单元测试 `remote_terminal_command_runs_through_app_bridge_client`：启动极小 HTTP mock server，直接调用 TUI backend `handle_command("/terminal printf tui-terminal-ok")`，断言请求命中 `POST /api/terminal/run`、带 Bearer auth、body 包含 command，并验证返回 timeline line 含 `tui-terminal-ok`。验证：`cargo fmt --all` 通过；`cargo test -p openagent-cli remote_terminal_command_runs_through_app_bridge_client --lib -- --nocapture` 通过；`cargo test -p openagent-cli binary_terminal_runs_remote_bridge_command --test cli_commands -- --nocapture` 通过；`cargo fmt --all -- --check` 通过；`cargo check -p openagent-cli -p openagent-app-server-client` 通过；无遗留 App Bridge/Vite 监听端口。残留：这仍不是 PTY/交互式终端，只是 TUI/attach 命令触发短命令 runner；真正 curses UI 没有单独截图验证；terminal 命令尚未接 approval/kill/streaming。
  - 2026-07-01: App Bridge terminal client + CLI entry slice 完成。把上一轮 Desktop-only 的 `POST /api/terminal/run` 推进为可被 CLI/TUI/Desktop 共用的 App Bridge client 能力，不改终端执行语义、不做 PTY、不推远端。`runtime/app-server-client/src/app_bridge_client.rs` 新增 `RemoteRuntimeClient::terminal_run(command, cwd, timeout_ms)`，封装 `POST /api/terminal/run`，并在 client fixture/golden 中加入 `terminal_run` request shape；同步当前 `AppEvent::to_value` 已输出 `event_id` 的 golden 字段。`cli/Cargo.toml` 新增 `openagent-app-server-client` workspace 依赖；`cli/src/remote.rs` 新增 `openagent terminal` 命令，支持 `--server-url`/`--attach`、Bearer/basic auth、`--cwd`/`--workspace`/`--dir`、`--timeout-ms`、`--format text|json`、`--command` 或 `-- <command...>`，实际 HTTP 请求复用 `RemoteRuntimeClient`；text 模式原样输出 remote stdout/stderr，JSON 模式输出完整 terminal payload，并把 CLI exit code 映射到远端命令 exit code。`cli/src/cli.rs` 和 `cli/src/help.rs` 注册 `terminal` 命令；`cli/tests/cli_commands.rs` 新增真实 App Bridge 集成测试，启动 `openagent serve` 后通过 CLI 跑 `printf cli-terminal-ok` 和 `pwd`，验证 stdout、JSON payload、`cwd_relative=nested`、exit code。验证：`cargo fmt --all` 通过；`cargo test -p openagent-cli binary_terminal_runs_remote_bridge_command --test cli_commands -- --nocapture` 通过；`cargo test -p openagent-app-server-client app_bridge_client_matches_legacy_oracle --test remote_runtime -- --nocapture` 通过；`cargo test -p openagent-cli binary_help_smoke_covers_legacy_command_surface --test cli_commands -- --nocapture` 通过；`cargo fmt --all -- --check` 通过；`cargo check -p openagent-cli -p openagent-app-server-client` 通过；无遗留 App Bridge/Vite 监听端口。残留：这仍是短命令 runner，不是 PTY/持续 streaming terminal；TUI 尚未加 terminal command 面板；其他 CLI 远端路径仍有旧手写 HTTP helper，后续应继续迁移到 `RemoteRuntimeClient`。
  - 2026-07-01: Desktop latest Codex reference visual polish slice 完成。根据 William 最新反馈“这个 UI 做的太丑，模仿 Codex”，本轮只收敛 Desktop 视觉层，不改 Rust runtime/CLI/TUI 语义、不推远端。`desktop/src/styles.css` 被整理为更贴近 Codex 截图的亮色桌面 client 视觉系统：左侧固定浅灰 sidebar、白色主阅读画布、克制 topbar、灰色 icon-only 状态、居中 timeline、底部浮动 composer、轻量错误状态、右侧 inspector drawer 和移动端单栏。相比上一轮进一步降低 dashboard/card 感，压低边框/阴影/状态色，保留现有 App Bridge/session/review/terminal UI 行为。验证：`npm --prefix desktop run build` 通过；Vite dev `127.0.0.1:5202` + system Chrome Playwright 截图验证，桌面 2048x1272 渲染稳定，sidebar 宽度与参考图接近，主画布无横向 overflow，composer 居中浮底，topbar 状态不再红绿抢眼；移动 390x844 截图无横向 overflow，topbar/composer/error 状态稳定；参考图通过 `view_image` 查看，渲染截图 `/tmp/openagent-codex-like-ui-v2.png`、`/tmp/openagent-codex-like-ui-mobile.png` 已人工对比。残留：预览未启动 App Bridge 时仍会显示轻量 `Failed to fetch`，真实会话/goal strip 需要后续接入 runtime 数据；本轮没有 packaged Tauri GUI 窗口点击验证。
  - 2026-07-01: Desktop Terminal Panel MVP slice 完成。新增 workspace-scoped 受控终端运行能力，推进 Desktop 右侧信息面板中的 terminal panel 目标，但明确不是完整 PTY/交互 shell。`runtime/http/src/http_runtime.rs` 新增 `POST /api/terminal/run`，protocol manifest 暴露 `terminal_run` endpoint；payload 接收 `command`、可选 `cwd`/`timeout_ms`，命令长度限制 4096 chars，默认 timeout 10s、最大 30s，stdout/stderr 各限制 20000 chars 并返回 truncated 标记；cwd 通过 `resolve_path_in_root` 限制在 App Bridge workspace 内，返回 command/workspace/cwd/cwd_relative/success/exit_code/timed_out/duration/stdout/stderr。新增单测 `app_bridge_terminal_run_is_workspace_scoped`：`printf terminal-ok` 在 `nested` cwd 成功，`cwd: ".."` 被 400 拒绝。`desktop/src/App.tsx` 新增 `TerminalRunResult` state、`runTerminalCommand` 和右侧 Overview Terminal card；Desktop 通过 `/api/terminal/run` 执行命令并展示 CWD/Exit/Time/stdout/stderr。`desktop/src/styles.css` 新增 terminal form/output 样式，stdout/stderr 使用紧凑 dark output block。验证：`cargo fmt --all` 通过；`cargo test -p openagent-http-runtime app_bridge_terminal_run_is_workspace_scoped -- --nocapture` 通过；`npm --prefix desktop run build` 通过；真实 rendered smoke 使用临时 workspace 启动 `openagent-http-runtime` 于 `127.0.0.1:8803`、Desktop Vite 于 `127.0.0.1:5201`，浏览器打开详情面板，填 Bridge URL，Terminal command 输入 `printf terminal-ui-ok` 并点击 Terminal form Run，`data-testid=terminal-output` 显示 `terminal-ui-ok`，页面含 Exit `0`，console logs 为空，截图 `/tmp/openagent-terminal-panel-smoke.png` 已人工查看。残留：这不是 PTY，不支持交互式 stdin、持续 streaming、后台 task attach、shell profile/env 管理或 approval；终端 API 目前是 Desktop 显式面板能力，尚未接 CLI/TUI app-server-client 封装。
  - 2026-07-01: Desktop Codex UI polish slice 完成。根据 William 最新截图反馈“这个 UI 太丑，模仿 Codex”，本轮只做 Desktop 主界面视觉/信息架构收敛，不改 runtime 语义、不推远端。`desktop/src/App.tsx` 新增 Codex-like 左上 fake window controls/back/forward chrome，并把 Bridge URL/Token 从主侧栏移入右侧 Overview 的 Connection card；侧栏底部只保留 profile，主导航不再暴露 raw connection settings。`desktop/src/styles.css` 调整为 Codex 风格 shell：响应式 `--rail-width`、更轻的灰色 sidebar、隐藏 raw project-path add form、隐藏 sidebar bridge settings、icon-only topbar status、居中 timeline、更柔和的 floating composer、底部 profile 贴底、Connection form 字段样式和窄屏单栏适配。验证：`npm --prefix desktop run build` 通过；Browser path 打开 Vite `127.0.0.1:5197`，页面 title `OpenAgent Desktop`，桌面默认 viewport console logs 为空；1440x920 视觉 QA 无横向 overflow，rail 320px，composer 850px 且位于 bottom dock；800x900 窄屏 QA rail display none、composer 768px、overflow false；详情面板可从 topbar 打开，Connection card 可见且 Bridge URL/Token 输入保留；截图 `/tmp/openagent-codex-ui-polish-desktop.png`、`/tmp/openagent-codex-ui-polish-narrow.png` 已人工查看。残留：当前截图仍是 Vite/web preview，未证明 packaged Tauri 真实窗口；空态没有 Codex 的真实消息/goal strip，因为还缺 goal/session 数据源接入；`Failed to fetch` 来自未启动 App Bridge 的预览状态，不是构建失败。
  - 2026-07-01: Structured Diff / Split Review slice 完成。`runtime/http/src/http_runtime.rs` 的 `public_file_change` 现在保留旧 `diff` 字符串，同时从原始 before/after 生成 `side_by_side` 结构化 payload：`old_label`、`new_label`、`row_count`、`truncated`、`omitted_rows`、`rows[{kind,old_line,new_line,old,new}]`，使用现有 LCS 路径，小文件行级对齐，大文件回退 full-file row 列表并按 `MAX_RENDERED_DIFF_LINES` 截断；`runtime/http/tests/http_runtime.rs` 的 file diff/undo/redo 测试新增断言：创建文件时 latest side-by-side 有 added `alpha`，edit 时 rows 有 removed `alpha` 和 added `beta`。Desktop Review 面板优先渲染 `latest.side_by_side.rows` 为两列 Before/After split diff，缺失时回退 unified diff；`desktop/src/styles.css` 增加 `.review-split-diff`、header、row、cell、line-no、added/removed 状态样式，并给 `.inspector.open` 增加打开样式兜底，修复严格 smoke 发现的 drawer DOM 存在但 bbox 仍在屏幕外的问题。验证：`cargo fmt --all` 通过；`npm --prefix desktop run build` 通过；`cargo test -p openagent-http-runtime remote_runtime_client_tracks_file_diff_undo_and_redo --test http_runtime -- --nocapture` 通过；`cargo test -p openagent-http-runtime app_bridge_trust_boundary_routes_list_approve_diff_and_restore_checkpoint -- --nocapture` 通过；`cargo build -p openagent-http-runtime` 通过；真实 Rust App Bridge + Vite Desktop + Playwright strict smoke 通过，`/api/sessions/{id}/diff` 有 side_by_side rows，Desktop 点击 dock 打开 Review，`.app-shell` 有 `inspector-visible`，inspector bbox `x=1202,width=380,right=1582`，split diff bbox `x=1228,width=328,right=1556`，`rowCount=2`、`addedRows=2`、unified fallback 未渲染、body overflow `0`、console/page/failedResponses 均 `0`，截图 `/tmp/openagent-desktop-split-diff-visible-smoke.png` 已人工查看。残留：line-level LCS 还不是语义/AST diff，不支持文件树多文件 side-by-side，也没有虚拟滚动；仍是 Vite shell smoke，不是 packaged Tauri GUI。
  - 2026-07-01: Desktop Managed Bridge Command Readiness slice 完成。`desktop/src-tauri/src/lib.rs` 中 `AppBridgeProcess::start` 不再只 sleep 120ms 后返回，而是通过 `wait_for_bridge_port` 在 4s 内轮询真实子进程状态和 `127.0.0.1:<port>` TCP 可连接性；若 runtime 启动即退出或端口迟迟不可用，会杀掉 child 并返回明确错误，避免 Desktop 点 Start 后前端 refresh 抢跑。新增 `managed_bridge_starts_restarts_and_stops_runtime` 单测：真实启动 `target/debug/openagent-http-runtime`，用临时 workspace/session-root/port/auth token 验证无 token `/api/health` 为 401、Bearer token 为 200 + ok，`status` 返回 running，`restart` 切换到第二个 workspace 后 health 仍通，`stop` 后端口关闭且 final status stopped。验证：`cargo build -p openagent-http-runtime` 通过；`npm --prefix desktop run build` 通过；`cargo test --manifest-path desktop/src-tauri/Cargo.toml managed_bridge_starts_restarts_and_stops_runtime -- --nocapture` 通过；`cargo test --manifest-path desktop/src-tauri/Cargo.toml -- --nocapture` 通过，2 tests；`npm --prefix desktop run tauri -- build --no-bundle` 通过，生成 release binary；`git diff --check -- desktop/src-tauri/src/lib.rs desktop/index.html desktop/public/favicon.svg desktop/src/App.tsx desktop/src/styles.css .goal/state.md progress.md` 通过。尝试启动 release binary 和 `.app/Contents/MacOS/openagent-desktop` 做 Computer Use GUI smoke，但当前环境 `get_app_state("OpenAgent")` 返回 `cgWindowNotFound`，已清理进程。残留：真实 packaged GUI 点击 Start/Restart 仍未被窗口级自动化证明；当前已由 Tauri command 层真实子进程测试覆盖 managed bridge 核心行为。
  - 2026-07-01: Desktop Real App Bridge Review/Restore smoke slice 完成。补齐上一轮 Codex-like UI / workspace dock / Review panel 的真实数据流验证：`desktop/index.html` 增加 favicon 引用，`desktop/public/favicon.svg` 提供轻量 app icon，避免 Vite/Chrome 自动 favicon 404 污染前端 smoke。验证：`cargo build -p openagent-http-runtime` 通过；`npm --prefix desktop run build` 通过；`git diff --check -- desktop/index.html desktop/public/favicon.svg desktop/src/App.tsx desktop/src/styles.css` 通过；Playwright 使用真实 Rust App Bridge 和真实 Desktop Vite，临时 workspace/session root，预置 `PLAN_ONLY` write approval，Desktop 页面点击 `Allow` 后文件 `notes.txt` 写入 `codex-real-ui\n`，dock 出现 diff/checkpoint，点击 dock 打开 `.review-panel`，Review 展示 `+codex-real-ui` diff 和 Checkpoint Browser；点击真实 `step_start` checkpoint Restore 后文件从 workspace 消失，UI 显示 `Restored ckpt_...` 和 `.review-checkpoint-row.restored`，全局 `/api/events` 回放 `checkpoint/restored`；桌面横向 overflow `0`、consoleIssues/pageErrors/failedResponses 均为 `0`，截图 `/tmp/openagent-desktop-real-review-bridge-smoke-v2.png` 已人工查看。残留：这仍是 Vite/web preview + 外部 runtime，不是真实 packaged Tauri managed bridge click；Review diff 仍是 unified diff 字符串，不是 side-by-side/AST diff。
  - 2026-07-01: Desktop Review Panel / Diff-Checkpoint Browser slice 完成。把上个 slice 的 workspace dock 接到右侧 Review 面板：`desktop/src/App.tsx` 新增 `inspectorMode`、Overview/Review tabs、`openReviewPanel`/`openOverviewPanel`，dock 的 diff/checkpoint 行可点击进入 Review；Review 面板展示 Pending/Undo/Checkpoints 摘要、Change Review diff preview、Undo/Redo、Checkpoint Browser restore 列表、Context 文件/Git/workspace 预览。`desktop/src/styles.css` 新增 inspector tabs、review summary/card/diff/checkpoint row 样式和移动端 inspector 宽度约束；dock 可点击状态增加 focus/hover 反馈。验证：`npm --prefix desktop run build` 通过；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css` 通过；Vite `127.0.0.1:5177` + system Chrome Playwright mocked App Bridge，点击 dock 第三行 diff 后 `.review-panel` 出现，Review tab active，桌面 inspector `380x914`、overflow 0，Review 包含 diff code、2 个 checkpoint restore row、context 文件预览；移动端 inspector x=10 width=370、overflow 0。截图 `/tmp/openagent-review-panel-click.png`、`/tmp/openagent-review-panel-mobile.png` 已人工查看。残留：仍是前端 review 面板 polish，未新增后端 diff AST/side-by-side diff API；真实 Tauri packaged click smoke 仍待做。
  - 2026-07-01: Desktop Workspace Dock / Trust-Diff-Checkpoint UI slice 完成。延续 Codex-like 桌面形态，把信任边界和变更闭环从右侧诊断抽屉带回主工作区：`desktop/src/App.tsx` 新增 `showWorkspaceDock`/latest patch/latest checkpoint 派生状态，composer 上方新增 `workspace-dock`，直接渲染 pending approval/question 的 Allow/Deny/Reply/Dismiss、latest diff 的 Undo/Redo、latest checkpoint 的 Restore；git 摘要继续留在右侧详情，不抢主操作路径。`desktop/src/styles.css` 增加 dock/overlay/has-dock 布局，给 timeline 预留底部空间，限制 dock 高度并支持移动端滚动；tool/patch/context/approval message cards 降低 dashboard 卡片感，使用左侧状态线更贴近 Codex conversation timeline。验证：`npm --prefix desktop run build` 通过；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css` 通过；Vite `127.0.0.1:5177` + system Chrome Playwright mock App Bridge 数据验证，mock 会话包含 user/tool/assistant message、pending approval、pending question、latest diff、latest checkpoint、git changes；最终 QA：桌面 dock 4 行、height 188、在 composer 上方、横向 overflow 0、approval/question/diff/checkpoint/tool card 均出现、git 不在 dock；移动端 overflow 0、dock height 178、composer width 358、dock 在 composer 上方。截图 `/tmp/openagent-workspace-dock-final.png`、`/tmp/openagent-workspace-dock-mobile-final.png` 已人工查看。残留：本轮未改 Rust runtime/API 语义；dock 是主工作区轻量入口，完整 diff viewer/file/git/terminal/MCP 仍在后续面板化；真实 Tauri packaged click smoke 仍待做。
  - 2026-07-01: Desktop Codex-like UI Shell slice 完成。根据 William 提供的 Codex/Zcode 产品形态截图，把 Desktop 从深色三栏 diagnostics dashboard 重塑为 Codex-like 桌面会话客户端：`desktop/src/App.tsx` 新增 inspector drawer 状态，左侧改为轻灰半透明导航/置顶/项目/会话/连接栏，中间改为极简 topbar + 居中 timeline + 底部浮动 composer，协议/Provider/Stream/Trust/Diff/Checkpoints/Files/Git 等原有能力迁入右侧按需详情抽屉；`desktop/src/styles.css` 重写为亮色 Codex-like 视觉系统，包含轻量 sidebar、白色 conversation canvas、圆形 send、紧凑状态 pill、右侧抽屉和移动端单栏。验证：`npm --prefix desktop run build` 通过；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css` 通过；Vite 预览 `127.0.0.1:5177` 用系统 Chrome + Playwright fallback 截图验证，桌面 1600x1000 无横向溢出，rail 424px、composer 850px 居中，详情抽屉可 toggle，移动 390px 无横向溢出；截图 `/tmp/openagent-codex-like-desktop-v2.png`、`/tmp/openagent-codex-like-inspector.png`、`/tmp/openagent-codex-like-mobile.png` 已人工查看。预览环境未启动 App Bridge，所以 console 有预期 `ERR_CONNECTION_REFUSED`，页面显示轻量 `Failed to fetch`，不代表构建失败。残留：这轮是视觉/信息架构改造，不是 chat message states 深度 polish；真实 Tauri packaged app click smoke 仍待做。
  - 2026-07-01: Desktop First-run Local Auth Token slice 完成。`desktop/src-tauri/Cargo.toml` 新增 `getrandom`；`desktop/src-tauri/src/lib.rs` 新增 `DesktopAuthToken`、`desktop_auth_token` Tauri command、`desktop_auth_token_path`/`openagent_home`/`generate_bridge_token`/`write_secret_file`，首次运行会生成 `oa_desktop_...` 本地 bridge token，写入 `OPENAGENT_DESKTOP_AUTH_TOKEN_PATH` 或默认 `~/.openagent/desktop/bridge-auth-token`，Unix 下用 `0600` 创建，后续启动复用同一个 token；command 已注册到 `tauri::generate_handler!`，并新增 `desktop_auth_token_persists_with_override` 单测证明第一次 created、第二次 local 复用同一 token。`desktop/src/App.tsx` 新增 `DesktopAuthToken` state，Tauri 初始化时调用 `desktop_auth_token` 并把 token 填入现有 Bearer auth 路径；initial refresh/SSE 在 Tauri token ready 前等待，managed bridge start/restart 继续复用 `managedBridgeStartOptions` 自动传 `authToken`；Bridge 区展示本地 token 文件路径但不展示 token 明文，Desktop inspector 新增 Auth 状态 `created/local/manual/none`，auth errors 走 diagnostics warning。验证：`cargo fmt --manifest-path desktop/src-tauri/Cargo.toml` 通过；`npm --prefix desktop run build` 通过；`cargo test --manifest-path desktop/src-tauri/Cargo.toml desktop_auth_token_persists_with_override -- --nocapture` 通过；`cargo check --manifest-path desktop/src-tauri/Cargo.toml` 通过；`npm --prefix desktop run tauri -- build --no-bundle` 通过，生成 `desktop/src-tauri/target/release/openagent-desktop`；HTTP auth smoke 启动 `openagent-http-runtime --auth-token oa_desktop_test_token` 于 `127.0.0.1:8802`，无 token `GET /api/health` 返回 401，Bearer token 返回 200 + `ok:true/service:openagent-http-runtime`。Browser path + web preview smoke：runtime 在 `127.0.0.1:8801`，Desktop Vite 在 `127.0.0.1:5200`，页面 title `OpenAgent Desktop`，DOM snapshot 含 Desktop，Desktop card 显示 `Auth none`（web preview 不生成 Tauri token），overlay false，page console warnings/errors empty；UI flow 填 Bridge URL `http://127.0.0.1:8801`、清空 Token、填 Project path `/Users/william/coding/harness/openharness`、Add Project、New session；API `GET /api/sessions` 确认最新 session workspace 为 `/Users/william/coding/harness/openharness`；临时服务已停止，`5200`/`8801`/`8802` 无监听，`desktop/src-tauri/gen/schemas` 已清理，`git diff --check -- desktop/src-tauri/Cargo.toml desktop/src-tauri/src/lib.rs desktop/src/App.tsx desktop/src/styles.css .goal/state.md progress.md` 通过。残留：真实 Tauri GUI 里尚未点击 Start/Restart 验证生成 token 被传入 managed child；当前 token 持久化用本地文件而非系统钥匙串；web preview 只能验证 auth UI 不退化。
  - 2026-07-01: Desktop Managed Bridge Restart Policy slice 完成。`desktop/src-tauri/src/lib.rs` 新增 `AppBridgeProcess::restart` 和 `app_bridge_restart` Tauri command，重启时先 stop 当前托管的 `openagent-http-runtime`，再用传入 workspace/session-root/port/auth token 启动新进程，并注册进 `tauri::generate_handler!`；`desktop/src/App.tsx` 抽出 `managedBridgeStartOptions`，Start/Restart 共用同一套 selected project workspace、session root、port、auth token 参数，新增 `restartManagedBridge` callback；Bridge workspace mismatch warning 现在带 `Restart` 按钮，Desktop inspector 也新增 `Restart` 按钮，并在任一 bridge 操作 busy 时统一禁用 Start/Restart/Stop/Status，避免并发点击；`desktop/src/styles.css` 给 warning action 增加窄栏布局和按钮样式。验证：`cargo fmt --manifest-path desktop/src-tauri/Cargo.toml` 通过；`npm --prefix desktop run build` 通过；`cargo check --manifest-path desktop/src-tauri/Cargo.toml` 通过；`npm --prefix desktop run tauri -- build --no-bundle` 通过，生成 `desktop/src-tauri/target/release/openagent-desktop`。Browser path + 临时 App Bridge smoke：runtime 在 `127.0.0.1:8800`，Desktop Vite 在 `127.0.0.1:5199`，页面 title `OpenAgent Desktop`，DOM snapshot 含 Desktop，Restart 控件唯一且在 web preview 下 disabled，overlay false，page console warnings/errors empty；UI flow 填 Bridge URL `http://127.0.0.1:8800` -> 填 Project path `/Users/william/coding/harness/openharness` -> Add Project -> New session；UI 显示 selected project/topbar workspace 为 `/Users/william/coding/harness/openharness`，API `GET /api/sessions` 确认最新 session workspace 为 `/Users/william/coding/harness/openharness`；临时服务已停止，`5199`/`8800` 无监听，`desktop/src-tauri/gen/schemas` 已清理，`git diff --check -- desktop/src-tauri/src/lib.rs desktop/src/App.tsx desktop/src/styles.css` 通过。残留：`app_bridge_restart` 已编译注册，但本轮仍未在真实 Tauri GUI 里点击 Start/Restart/Stop；web preview 只能验证 disabled 状态和 UI 不回退；first-run auth token 仍待做。
  - 2026-07-01: Desktop Native Folder Picker / Bridge Workspace Hint slice 完成。`desktop/src-tauri/Cargo.toml` 引入 `rfd`，`desktop/src-tauri/src/lib.rs` 新增 `choose_project_folder` Tauri command，并复用 `project_path_info_for_input` 做 canonical path/name/dir validation；`desktop/src/App.tsx` 左侧项目 form 增加原生 folder picker 按钮，真实 Tauri 下调用 native dialog，web preview 下禁用但保留手动路径输入；Desktop Bridge 区新增 managed bridge workspace mismatch 提示，避免用户切项目后误以为当前 bridge 已经切到新 workspace；`desktop/src/styles.css` 调整三列项目输入布局并补 warning 样式。验证：`cargo fmt --manifest-path desktop/src-tauri/Cargo.toml` 通过；`cargo check --manifest-path desktop/src-tauri/Cargo.toml` 通过；`npm --prefix desktop run build` 通过；`npm --prefix desktop run tauri -- build --no-bundle` 通过，生成 `desktop/src-tauri/target/release/openagent-desktop`。Browser path + 临时 App Bridge smoke：runtime 在 `127.0.0.1:8799`，Desktop Vite 在 `127.0.0.1:5198`，页面 title `OpenAgent Desktop`，URL/Project/Choose/Add/New session 控件均唯一，Choose 在 web preview 下 disabled，overlay false，console warnings/errors empty；UI flow 填 Bridge URL `http://127.0.0.1:8799` -> 填 Project path `/Users/william/coding/harness/openharness` -> Add Project -> New session；UI 显示 selected project/topbar workspace 为 `/Users/william/coding/harness/openharness`，API `GET /api/sessions` 确认最新 session workspace 为 `/Users/william/coding/harness/openharness`；临时服务已停止，`5198`/`8799` 无监听；Tauri 生成的 `desktop/src-tauri/gen/schemas` 已清理。残留：原生 folder dialog 命令已编译注册，但本轮仍未在真实 packaged Tauri GUI 中点击；mismatch 目前是提示，不会自动 restart bridge；first-run auth token/auto-start policy 仍待做。
  - 2026-07-01: Desktop Project Registry / Workspace Selection slice 完成。Desktop 左侧项目区不再硬编码 `openharness`：`desktop/src/App.tsx` 新增 `DesktopProject` registry、localStorage 持久化、项目路径输入、Add Project、项目 row 选择、project-scoped session list、选中 workspace topbar 展示；`New session` 现在向 `POST /api/sessions` 发送 `cwd: selectedProjectPath`，managed bridge start 也优先使用 selected project 作为 workspace。`desktop/src-tauri/src/lib.rs` 新增 `project_path_info` Tauri command，支持 `~` 展开、exists/is_dir、canonical path、display name 和 error，真实 Tauri 下 Add Project 可用该命令验证路径；Vite/web preview 下保留手动登记以方便 smoke/dev。`desktop/src/styles.css` 增加 compact project input/list/error 样式。验证：`cargo fmt --manifest-path desktop/src-tauri/Cargo.toml` 通过；`cargo check --manifest-path desktop/src-tauri/Cargo.toml` 通过；`npm --prefix desktop run build` 通过；`npm --prefix desktop run tauri -- build --no-bundle` 通过；Browser path + 临时 App Bridge smoke：runtime 在 `127.0.0.1:8798`，Desktop Vite 在 `127.0.0.1:5197`，页面 `OpenAgent Desktop` 非空、overlay false、console warnings/errors empty；UI flow 填 Bridge URL `http://127.0.0.1:8798` -> 填 Project path `/Users/william/coding/harness/openharness` -> Add Project -> project row selected -> New session；UI 显示 selected project `openharness`、topbar workspace `/Users/william/coding/harness/openharness`、单个 `openharness session`；`GET /api/sessions` 确认 created session workspace 为 `/Users/william/coding/harness/openharness`；临时 Vite/App Bridge 已停止，`5197`/`8798` 无监听；`git diff --check -- desktop/src-tauri/src/lib.rs desktop/src/App.tsx desktop/src/styles.css progress.md .goal/state.md` 通过。残留：仍是手动路径输入，不是 native folder picker/onboarding；`project_path_info` 未在真实 Tauri GUI 中点击验证；App Bridge workspace 切换仍需要 start/restart，没有 auto-start/migration policy。
  - 2026-07-01: Desktop-managed App Bridge process slice 完成。Tauri Desktop shell 现在能托管本地 Rust App Bridge 进程，而不是只指向外部 URL：`desktop/src-tauri/src/lib.rs` 新增 `AppBridgeProcess` managed state、`app_bridge_status`/`app_bridge_start`/`app_bridge_stop` commands、headless `openagent-http-runtime` 启动参数、workspace/session-root/auth option 传递、pid/url/workspace/session root 状态返回、child cleanup on stop/drop，并把 bridge discovery 扩展到 repo `target/release`；`desktop/src/App.tsx` 新增 `ManagedBridgeStatus`、managed bridge state、Start/Stop/Status handlers 和 Desktop inspector 控制，启动成功会把 managed URL 写回 Bridge URL，Vite/web preview 下按钮禁用并显示 `Managed web preview`。验证：`cargo fmt --manifest-path desktop/src-tauri/Cargo.toml` 通过；`cargo check --manifest-path desktop/src-tauri/Cargo.toml` 通过；`npm --prefix desktop run build` 通过；`npm --prefix desktop run tauri -- build --no-bundle` 通过并生成 release app binary；`target/debug/openagent-http-runtime` 存在于默认发现路径；手动用 Desktop 同款参数启动 runtime 在 `127.0.0.1:8797`，`curl -i /api/health` 返回 HTTP 200 和 `ok:true/service:openagent-http-runtime/ui_enabled:false`，随后停止；Browser path 打开 `http://127.0.0.1:5196/`，页面 `OpenAgent Desktop` 非空、overlay false、console warnings/errors empty，Desktop card 显示 `Runtime web preview`、`Managed web preview`，Start/Stop/Status 均 disabled；Vite server 已停止；`git diff --check -- desktop/src-tauri/src/lib.rs desktop/src/App.tsx desktop/src/styles.css progress.md .goal/state.md` 通过。残留：Tauri command 的 Start/Stop 还没在实际 GUI 会话里点击；未做 project picker/onboarding、auto-start policy、auth token generation、signed bundle/auto-update。
  - 2026-07-01: Desktop First-run Diagnostics / Tauri Shell slice 完成。`desktop/src-tauri` 现在是可编译的 Tauri v2 crate：补齐 `src/lib.rs`、标准 `build.rs`、最小 `main.rs`、独立 `[workspace]`、`serde` 依赖和临时 PNG app icon；`tauri::generate_context!()` 不再因为缺 lib/build script/icon 失败。Tauri 新增 `desktop_diagnostics` command，返回 runtime/app version/os/arch/default bridge URL/session root、`OPENAGENT_HTTP_RUNTIME`/bundle/repo target/PATH 的 `openagent-http-runtime` binary discovery 和 warnings。React Desktop 新增 Desktop inspector card：真实 Tauri 下调用 diagnostics，Vite/web preview 下显示 `Runtime web preview`、`Bridge external`，不触发 invoke error；`desktop/src/styles.css` 增加 diagnostics warning 样式。验证：`cargo check --manifest-path desktop/src-tauri/Cargo.toml` 通过；`npm --prefix desktop run build` 通过；`npm --prefix desktop run tauri -- info` 能识别 tauri 2.11.4 / tauri-build 2.6.3 / wry 0.55.1 / tao 0.35.3，仍提示 Xcode app 缺失和 rustup 未安装；`npm --prefix desktop run tauri -- build --no-bundle` 通过，生成 `/Users/william/coding/harness/openharness/desktop/src-tauri/target/release/openagent-desktop`；Browser path 打开 `http://127.0.0.1:5195/`，页面 `OpenAgent Desktop` 非空、Vite overlay false、console warnings/errors empty，Desktop card 显示 `Runtime web preview`、`Bridge external`、default URL `http://127.0.0.1:8787`；`git diff --check -- desktop/src-tauri/Cargo.toml desktop/src-tauri/build.rs desktop/src-tauri/src/lib.rs desktop/src-tauri/src/main.rs desktop/src-tauri/icons/icon.png desktop/src/App.tsx desktop/src/styles.css progress.md .goal/state.md` 通过。残留：full macOS `.app`/DMG signing/bundling 未证明，本机 `tauri info` 仍显示 Xcode app missing；Desktop 还不会自动启动/管理 Rust App Bridge 进程；图标是临时 placeholder。
  - 2026-07-01: App Bridge Runtime Event ID slice 完成。App Bridge events 现在携带 runtime-issued `event_id`，由 `session_id + turn_id + sequence` 生成；`/api/protocol` 声明 `event_id` 是首选事件身份字段，`global_sequence`/`sequence` 继续作为 SSE resume cursor；blocking `POST /turns` response events 和 live `/api/events` SSE replay 对同一个事件暴露相同 `event_id`。实现：`runtime/app-server/src/app_bridge.rs` 给 `AppEvent` 增加 optional `event_id`；`runtime/http/src/http_runtime.rs` 增加 `normalize_app_event`/`app_event_id`，`append_app_events`/`append_unpersisted_app_events` 改为持久化前就地归一化，并更新 protocol manifest；`runtime/app-server-client/src/app_bridge_client.rs` 解析 `event_id` 并在 `RemoteEventKey` 中优先使用；`runtime/tui/src/events.rs`、`desktop/src/App.tsx` 和 `runtime/static/app-server/static/app.js` 都优先用 `event_id` dedupe，旧事件 fallback 到原 key。验证：`cargo fmt --all` 通过；`npm --prefix desktop run build` 通过；`cargo test -p openagent-http-runtime app_bridge_protocol_contract_and_client_live_subscription --test http_runtime -- --nocapture` 通过，并断言 protocol required fields 含 `event_id`、turn response/live global SSE/turn SSE 的 `turn/completed` event_id 一致；`cargo test -p openagent-app-server-client --lib -- --nocapture` 通过；`cargo build -p openagent-http-runtime` 通过；`cargo test -p openagent-tui --lib -- --nocapture` 通过，51 tests；`cargo test -p openagent-http-runtime global_sse_live_tails_provider_stream_delta_before_completion --test http_runtime -- --nocapture` 通过；`cargo test -p openagent-app-server --lib -- --nocapture` 通过；`git diff --check -- runtime/app-server/src/app_bridge.rs runtime/app-server-client/src/app_bridge_client.rs runtime/http/src/http_runtime.rs runtime/http/tests/http_runtime.rs runtime/tui/src/events.rs runtime/static/app-server/static/app.js desktop/src/App.tsx progress.md .goal/state.md` 通过。残留：旧 session JSONL 没有 `event_id` 时仍依赖 fallback；`event_id` 基于 append-only turn sequence，未来若支持历史中间插入事件，需要兼容策略；本轮不是 packaged Tauri 验证。
  - 2026-07-01: Desktop Event Identity Normalization slice 完成。Desktop timeline/live event list 现在使用 semantic event identity 去重，避免同一个 turn 的事件先通过 live `/api/events` 到达、再通过 blocking `POST /turns` response 返回时重复渲染。`desktop/src/App.tsx` 新增 `eventTurnId`、`stableJson`、`eventSemanticKey`；`eventKey` 改为 semantic key 优先，字段不足时再回退 `global_sequence` / `sequence`，保留旧事件兼容。验证：`npm --prefix desktop run build` 通过；`cargo build -p openagent-http-runtime` 通过；Browser path 使用 fake Responses streaming provider `127.0.0.1:19094`、Rust App Bridge `127.0.0.1:18814`、Vite Desktop `127.0.0.1:5194`，页面 `OpenAgent Desktop` 非空、Vite overlay false、console warnings/errors empty。Flow：`New session -> event identity smoke -> live streaming -> completion`；provider request count 1；`/api/sessions` 显示一个 idle session 且 `message_count: 2`；Desktop event rows 正好 4 条：`started #1`、`agentMessage delta #2 identity`、`agentMessage delta #3 complete`、`completed #4 identity complete`，没有重复 returned-payload rows；Stream inspector 显示 `Events 4`、`Messages 2`、`Cursor 4`、`Resume 4`。截图：`/tmp/openagent-desktop-event-identity-smoke.jpg`。残留：这是 Desktop-side normalization，长期应由 runtime/protocol 提供稳定 `event_id`；semantic key 理论上可能折叠同一 turn 内完全相同 method/params 的重复事件；本轮仍是 Vite shell smoke，不是 packaged Tauri。
  - 2026-07-01: Desktop Stream Status Semantics slice 完成。Desktop 现在区分 Agent run state 与 App Bridge event transport state：顶部状态 pill 用 `idle/running/streaming/waiting_approval/waiting_question/failed`，右侧 Stream inspector 用 `polling/receiving/listening/reconnecting`，空闲长轮询不再误显示 `connecting`。`desktop/src/App.tsx` 新增 `SseEventHandler` 与 `streamStateAfterEvents`；`readSse` 支持 frame-level callback，SSE frame 到达即 append event、刷新 run state 和 session/trust 数据；live event loop 开始请求时标为 `polling`，收到帧标为 `receiving`，成功空闲为 `listening/resumed`，断线才 `reconnecting`；`submitPrompt` 用返回事件计算 run state，不再 finally 无条件改 idle；顶部 stream pill 接入 `statusClass(streamState)`。验证：`npm --prefix desktop run build` 通过；`cargo build -p openagent-http-runtime` 通过；`git diff --check -- desktop/src/App.tsx desktop/src/styles.css progress.md .goal/state.md` 通过；Browser path 使用 fake Responses streaming provider `127.0.0.1:19093`、Rust App Bridge `127.0.0.1:18813`、Vite Desktop `127.0.0.1:5193`，页面 `OpenAgent Desktop` 非空、Vite overlay false、console warnings/errors empty。Idle state：Bridge 在线后顶部 `idle`、Stream inspector 恢复到 `polling/listening` 风格状态而非 `connecting`；Live state：provider 仍在流式输出时顶部 `streaming`、Stream inspector `receiving`、timeline 可见 partial delta `stream`；Final state：完成后顶部 `idle`、Stream inspector `polling`、assistant final text `stream status complete`、session `idle`、provider request count 1。截图：`/tmp/openagent-desktop-stream-status-smoke.jpg`。残留：App Bridge 仍是 bounded live SSE/long-poll window，不是永久 EventSource/WebSocket；timeline 仍可能同时显示 live events 与 POST payload returned events 的重复，需要后续 event identity normalization；本轮仍是 Vite shell smoke，不是 packaged Tauri。
  - 2026-07-01: Desktop Trust History Cards slice 完成。Desktop approval/question 不再只是 generic JSON/message-part trace：timeline 和 Trust inspector 都渲染专用 `TrustHistoryCard`，Recent history dock 会从持久化 message parts 恢复最近 approval/question 记录。`desktop/src/App.tsx` 新增 `TrustHistoryItem`、approval/question part 解析、`trustHistoryFromMessages`、approval/question timeline card 替换和 Trust Recent history；`desktop/src/styles.css` 新增 trust history dock/timeline 样式、ok/warn/bad tone 和 approval/question card styles。验证：`npm --prefix desktop run build` 通过；`cargo build -p openagent-http-runtime` 通过；Browser path 使用 fake Responses provider `127.0.0.1:19092`、Rust App Bridge `127.0.0.1:18812`、Vite Desktop `127.0.0.1:5192`。Approval Allow flow 完成 `New session -> approval history desktop smoke -> pending bash approval -> Allow`，session 回到 idle，`/api/approvals` count 0，`trust-history.txt` 被创建，message v2 含 `approval pending` 和 `approval completed/allowed`，timeline/dock 均有 `data-part-kind="approval"` + `data-interaction-status="allowed"`。Question Reply flow 完成 `New session -> question history desktop smoke v2 -> pending question -> Reply`，session 回到 idle，`/api/questions` count 0，message v2 含 `question pending` 和 `question completed/answered`，timeline/dock 均有 `data-part-kind="question"` + `data-interaction-status="answered"`。截图：`/tmp/openagent-desktop-trust-history-approval.jpg`、`/tmp/openagent-desktop-trust-history-question.jpg`。残留：仍是 Vite shell smoke，不是 packaged Tauri；trust history 已产品化但还不是底部/floating approval dock；stream inspector 仍是 long-poll `connecting` 语义；未来 question smoke provider 需使用完整 `questions[]` schema，薄 `{question: ...}` payload 会导致 reply 400。
  - 2026-07-01: Desktop Deny / Dismiss rendered smoke slice 完成。使用当前构建的 Rust App Bridge binary 验证 Desktop 负向信任边界：`npm --prefix desktop run build` 通过；`cargo build -p openagent-http-runtime` 通过；fake OpenAI-compatible provider `127.0.0.1:19091`、Rust App Bridge `127.0.0.1:18811`、Vite Desktop `127.0.0.1:5191`。Browser path 成功，页面 `OpenAgent Desktop` 非空、Vite overlay false、console warnings/errors empty。Approval Deny flow：`New session -> approval deny desktop smoke current -> pending bash approval -> Deny` 后 Trust 显示 `CLEAR`/`No pending interaction`、session idle、Deny button 消失、timeline cards 为 `tool/approval/approval` 且 denied trace 可见；API 侧 `/api/approvals` count 0，`denied-ui.txt` 未创建，messages v2 含 `tool pending`、`approval pending`、`approval error` 且 `content_status: denied`。Question Dismiss flow：`New session -> question dismiss desktop smoke current -> pending question -> Dismiss` 后 Trust 显示 `CLEAR`/`No pending interaction`、session idle、Dismiss button 消失、timeline cards 为 `tool/question/question` 且 dismissed trace 可见；API 侧 `/api/questions` count 0，messages v2 含 `tool pending`、`question pending`、`question error` 且 `content_status: dismissed`。截图：`/tmp/openagent-desktop-approval-deny-smoke.png`、`/tmp/openagent-desktop-question-dismiss-smoke.png`。残留：approval/question history 仍是 generic append-only cards，不是 polished trust-history dock；stream inspector 的 `connecting` 仍是 long-poll 语义；本轮验证的是 Vite Desktop shell，不是 packaged Tauri。
  - 2026-07-01: Approval deny / question dismiss runtime slice 完成。App Bridge 非 resume 交互响应补齐顶层状态：approval deny 返回 `status: "failed"`，question dismiss 返回 `status: "failed"`，非 provider question answer 返回 `status: "completed"`，方便 CLI/TUI/Desktop 统一消费。新增 dedicated tests：`remote_runtime_client_stops_provider_after_approval_deny` 证明 deny 后 `turn/approval_resolved` 为 denied、`turn/failed` 为 approval denied、provider 请求数保持 1、bash 命令没有创建 `denied.txt`、`pending_approval`/`pending_provider_turn` 清空、session 回到 idle、message v2 同时保留 pending approval part 和 error/denied resolution part；`remote_runtime_client_stops_provider_after_question_dismiss` 证明 dismiss 后 `item/question/resolved` 为 dismissed、`turn/failed` 使用 dismiss note、provider 请求数保持 1、`pending_question`/`pending_provider_turn` 清空、session 回到 idle、message v2 同时保留 pending question part 和 error/dismissed resolution part。验证：`cargo fmt --all` 通过；`cargo test -p openagent-http-runtime remote_runtime_client_stops_provider_after_approval_deny --test http_runtime -- --nocapture` 通过；`cargo test -p openagent-http-runtime remote_runtime_client_stops_provider_after_question_dismiss --test http_runtime -- --nocapture` 通过；`cargo test -p openagent-http-runtime remote_runtime_client_resumes_provider_after_approval_allow --test http_runtime -- --nocapture` 通过；`cargo test -p openagent-http-runtime remote_runtime_client_resumes_provider_after_question_reply --test http_runtime -- --nocapture` 通过。残留：deny/dismiss 还没有 Desktop rendered smoke；approval/question timeline 仍是 append-only trace cards，不是 polished trust-history dock；stream `connecting` 文案/真实传输层仍需后续产品化。
  - 2026-07-01: Desktop question flow rendered smoke slice 完成。修复 session replay 中 pending/running `tool` part 的错误中断判定：当同 call_id 的 pending/running approval/question part 仍在等待用户响应时，tool part 保持 pending，而不是被标为 `interrupted`；新增 `file_session_store_keeps_tool_call_pending_while_waiting_for_question` 回归测试，同时保留原有 unfinished tool replay 会 interrupted 的行为测试。验证：`cargo fmt --all` 通过；`cargo test -p openagent-session file_session_store_keeps_tool_call_pending_while_waiting_for_question --test session_trace -- --nocapture` 通过；`cargo test -p openagent-session file_session_store_marks_unfinished_tool_calls_interrupted_on_replay --test session_trace -- --nocapture` 通过；`cargo test -p openagent-http-runtime remote_runtime_client_resumes_provider_after_question_reply --test http_runtime -- --nocapture` 通过。Rendered Desktop smoke 复用 fake provider `127.0.0.1:19081`、Rust App Bridge `127.0.0.1:18801`、Vite `127.0.0.1:5186`；Browser path 成功，页面 `OpenAgent Desktop` 非空、Vite overlay false、console warnings/errors empty、final answer `question-smoke-completed` 可见、Trust 显示 `CLEAR`/`No pending interaction`、页面无 `interrupted`；`/api/questions` count 为 0，session 为 idle，messages v2 含 `tool pending`、`question pending`、`tool completed`、`question completed/answered`。截图：`/tmp/openagent-desktop-question-final.png`。残留：question timeline 仍是 append-only trace card，需要 polished history/dock；approval deny 和 question dismiss 仍缺 dedicated tests；stream `connecting` 文案/传输还需后续产品化。
  - 2026-07-01: Interaction resolved parts slice 完成。Rust HTTP runtime 现在会给 pending question payload 写入 `assistant_message_id`，并在 approval allow / question reply 后追加 completed `approval` / `question` message part，content 中保留 `request` 与 `resolution`，让 session restore 后能看到信任边界从 pending 到 resolved 的完整轨迹；approval deny 和 question dismiss 也复用同一 helper 写 error-status part（当 assistant message 已存在时）。验证：`cargo fmt --all` 通过；`cargo test -p openagent-http-runtime app_bridge_trust_boundary_routes_list_approve_diff_and_restore_checkpoint -- --nocapture` 通过，新增 standalone approval completed part 断言；`cargo test -p openagent-http-runtime remote_runtime_client_resumes_provider_after_approval_allow --test http_runtime -- --nocapture` 通过，验证 provider approval pending+allowed parts；`cargo test -p openagent-http-runtime remote_runtime_client_resumes_provider_after_question_reply --test http_runtime -- --nocapture` 通过，验证 provider question pending+answered parts。残留：deny/dismiss 分支共用 helper 但缺专门测试；question flow 仍需要 rendered Desktop smoke；approval/question timeline 仍是 generic part cards，不是 polished history view。
  - 2026-07-01: Approval-resume runtime consistency slice 完成。Rust HTTP runtime 现在会在 approval pause 前把 `assistant_message_id` 和 `snapshot_start` 写入 pending approval；standalone `tool_call` approval allow 后会追加 assistant completion message，并生成 `step_end` checkpoint、assistant `context(checkpoint)` part 和 `patch` part；provider-loop approval allow 后会先 finalize 当前 provider step 的 checkpoint/parts，再恢复 provider 下一步，避免真实 Agent Loop 的批准恢复丢失 trace。普通 direct tool turn 和 approval allow 路径复用 `append_runtime_completion_assistant`，减少后续漂移。验证：`cargo fmt --all` 通过；`cargo test -p openagent-http-runtime app_bridge_trust_boundary_routes_list_approve_diff_and_restore_checkpoint -- --nocapture` 通过，新增断言 approval 后 messages_v2 包含 user/tool/assistant，approval assistant parts 含唯一 id、`context(checkpoint)` 和 `patch`，checkpoint 列表含 `step_end`；`cargo test -p openagent-http-runtime remote_runtime_client_round_trips_tui_approval --test http_runtime -- --nocapture` 通过；`cargo test -p openagent-http-runtime remote_runtime_client_resumes_provider_after_approval_allow --test http_runtime -- --nocapture` 通过。残留：approval/question pending message parts 还没有原地从 pending 更新为 resolved；question flow 仍需要 rendered smoke；本轮没有改前端，因此未跑 Desktop build。
  - 2026-07-01: Desktop checkpoint restore end-to-end rollback slice 完成。Desktop Checkpoints 面板新增 restore 可见状态：`restoringCheckpointId`/`restoredCheckpointId`，restore 按钮执行时禁用，恢复后面板显示 `restored` badge、`Restored <checkpoint>`，被恢复的 checkpoint row 高亮；restore 响应复用 `refreshFromEvents`，与 SSE 事件驱动刷新路径一致。`/api/files?path=...&content=true` 对“路径合法但文件已被 checkpoint restore 删除”的情况不再返回 400，而是返回 200 + `exists:false` + `error:"not found"`；Files 面板显示 `<path> no longer exists`，避免回滚后的正常状态污染浏览器 console。HTTP runtime 目标测试补强：checkpoint restore 后全局 `/api/events?last_event_id=0` 可回放 `checkpoint/restored`。验证：`cargo fmt --all` 通过；`npm --prefix desktop run build` 通过；`cargo test -p openagent-http-runtime app_bridge_trust_boundary_routes_list_approve_diff_and_restore_checkpoint -- --nocapture` 通过；内置 Browser 打开本地 Vite 页面 20s 超时并重置，按前端验证 fallback 使用本地 Playwright；Playwright smoke 使用临时 App Bridge `127.0.0.1:18798` + Vite `127.0.0.1:5182`，预置 `PLAN_ONLY` write approval，Desktop 显示 pending 后点击 Allow，文件 `rollback.txt` 内容变为 `restore-me-2\n`，右侧 diff/checkpoint 可见，再点击 `step_start` checkpoint restore，文件从 workspace 消失，UI 显示 `Restored ckpt_...`、1 个 `.checkpoint-row.restored`、Files 面板显示 `rollback.txt no longer exists`，Trust 保持 clear，Vite overlay false、consoleIssues 0、pageErrors 0，截图 `/tmp/openagent-desktop-restore-e2e-smoke.png` 已检查；临时服务已停止。残留：approval-resume 路径当前只有 rollback 所需的 `step_start` checkpoint，没有像 direct tool turn 那样补齐 `step_end` checkpoint/assistant checkpoint part；这是下一步 runtime consistency slice。
  - 2026-07-01: Desktop approval live interaction slice 完成。Desktop 新增轻量 interaction sync：`/api/events` 每批事件进入后会识别 `turn/approval_requested`、`turn/approval_resolved`、`item/question/requested`、`item/question/resolved` 等信任边界事件，并自动刷新 `/api/approvals`、`/api/questions`、sessions、当前 session messages/diff/checkpoints/files/git；Allow/Deny/Reply/Dismiss 按钮响应路径也复用同一 `refreshFromEvents`，避免点击后 session 状态停留在 paused。Trust 面板新增 pending/clear badge、最近 sync 来源和按钮 busy/disabled 状态。HTTP runtime 目标测试补强：approval allow 后 pending approvals count 变为 0，global SSE 事件包含 `turn/approval_requested`、`turn/approval_resolved`、`item/toolCall/completed`。验证：`cargo fmt --all` 通过；`npm --prefix desktop run build` 通过；`cargo test -p openagent-http-runtime app_bridge_trust_boundary_routes_list_approve_diff_and_restore_checkpoint -- --nocapture` 通过；内置 Browser 打开本地 Vite 页面 20s 超时并重置，按前端验证 fallback 使用本地 Playwright；Playwright smoke 使用临时 App Bridge `127.0.0.1:18797` + Vite `127.0.0.1:5181`，预置 `PLAN_ONLY` write approval，Desktop 初始显示 `1 pending`、`approval2.txt` 和 diff preview，点击 Allow 后 Trust 显示 `clear`/`No pending interaction`，文件 `/tmp/openagent-desktop-approval-smoke/workspace/approval2.txt` 内容为 `approved-live-2\n`，选中 session 行从 `paused` 更新为 `idle`，右侧 diff/checkpoints 可见，Vite overlay false、consoleIssues 0、pageErrors 0，截图 `/tmp/openagent-desktop-approval-live-smoke.png` 已检查；临时服务已停止。残留：question flow 有同一刷新路径和按钮状态，但本轮 rendered smoke 只覆盖 approval；Trust 面板仍是右侧卡片，不是底部/浮层 dock；stream 仍是 long-poll 风格，截图中可能显示 connecting。
  - 2026-07-01: Desktop tool/patch/checkpoint cards slice 完成。Desktop timeline 不再把非 text message parts 混成裸 JSON：`text` part 继续作为正文渲染，`tool`/`patch`/`context(checkpoint)` parts 渲染为结构化 cards；Tool card 展示工具名、call id、状态和输出；Checkpoint card 展示 start/end checkpoint；Patch card 展示 added/modified/deleted、from/to checkpoint 和变更文件路径。修复 session store 默认 `part_id` 用毫秒 ID 导致同毫秒 context/patch part 撞 ID 的问题，改为基于 owner id + seq + kind 的稳定 part id；Desktop card key 也增加 kind/index 兜底旧数据。HTTP runtime 目标测试新增断言：direct tool flow 的 tool message 带 `tool` part，assistant message 带 `patch` 和 `context(checkpoint)` part，且 assistant parts id 唯一。验证：`cargo fmt --all` 通过；`npm --prefix desktop run build` 通过；`cargo test -p openagent-http-runtime app_bridge_trust_boundary_routes_list_approve_diff_and_restore_checkpoint -- --nocapture` 通过；内置 Browser 路径在打开本地 Vite 页面时 20s 超时并重置，按前端验证 fallback 使用本地 Playwright；Playwright smoke 使用临时 App Bridge `127.0.0.1:18796` + Vite `127.0.0.1:5180`，预置 FULL bash tool session 写入 `tool-card.txt` 并输出 `tool-card-output-unique`，Desktop 渲染 3 张 `.message-part-card`，kind 分别为 `tool/context/patch`，页面包含 tool 输出、`Checkpoint`、`Patch` 和 `tool-card.txt`，Vite overlay false、consoleIssues 0、pageErrors 0，截图 `/tmp/openagent-desktop-tool-cards-smoke.png` 已检查；临时服务已停止。残留：cards 仍是 timeline 内的轻量 inspector，不是可展开 trace panel；patch 还不是完整 diff viewer；stream 状态截图中仍可能处于 connecting long-poll 状态。
  - 2026-07-01: Desktop SSE reconnect/resume 状态语义 slice 完成。Desktop 新增 `StreamHealth` 状态模型，记录 `status`、`resume_cursor`、`reconnect_attempts`、`recovered_count`、`last_batch_count`、`last_error`、`next_retry_ms`；SSE loop 使用 `lastGlobalId.current` 作为 resume cursor，请求失败时进入 `reconnecting` 并指数式小退避，恢复成功后清空 transient stream error、保留 `recovered_count`，不再把临时 `/api/events` 错误写入全局 error banner；Stream inspector 展示 Status/Events/Messages/Cursor/Resume/Attempts/Recovered/Batch/Session 和 last stream error；顶部状态类支持 `connecting`/`reconnecting`/`resumed`。Rust targeted test 增加 `/api/events?last_event_id=` resume 断言，证明 cursor 后只返回后续事件。验证：`cargo fmt --all` 通过；`npm --prefix desktop run build` 通过；`cargo test -p openagent-http-runtime app_bridge_trust_boundary_routes_list_approve_diff_and_restore_checkpoint -- --nocapture` 通过；内置 Browser 连接成功但导航/快照阶段 20s 超时并重置，按前端验证 fallback 使用本地 Playwright；Playwright smoke 使用临时 App Bridge `127.0.0.1:18795` + Vite `127.0.0.1:5179`，预置 completed tool session 后让前两次 `/api/events` 返回 503，随后放行，Desktop 最终显示 persisted messages + live events，Stream 面板 `Events4 Messages3 Cursor4 Resume4 Attempts0 Recovered1 Batch4`，Vite overlay 0、page errors 0，仅忽略预期的 503 resource log，截图 `/tmp/openagent-desktop-sse-resume-smoke.png`；临时服务已停止。残留：这仍是客户端恢复状态/轮询式 long-poll，不是无限 HTTP/2/WebSocket stream；没有加 turn-scoped stream UI；没有把 retry/resume contract 写入正式 docs。
  - 2026-07-01: Desktop message persistence/recovery slice 完成。Desktop 新增 `SessionMessagesPayload`/message v2 类型和 `refreshSessionMessages`，active session 切换、prompt submit、approval/question response、undo/redo、checkpoint restore 后都会刷新 `/api/sessions/{id}/messages?limit=100`；timeline 从“只渲染 live events”升级为先渲染 Rust session store 的持久化 message rows，再渲染 live event rows；Stream inspector 显示 persisted Messages 数量。修复 Rust `run_http_tool_turn` 直连工具流没有持久化 tool result message 的问题：改为复用 `append_completed_tool_result`，并把 assistant message id/index 放到 tool messages 之后；目标测试新增 `/messages` 断言，覆盖 approval 后 user/tool transcript 与 direct tool turn 后 user/tool/assistant transcript。验证：`cargo fmt --all` 通过；`cargo test -p openagent-http-runtime app_bridge_trust_boundary_routes_list_approve_diff_and_restore_checkpoint -- --nocapture` 通过；`npm --prefix desktop run build` 通过；内置 Browser 连接成功但导航/快照阶段 20s 超时并重置，按前端验证 fallback 使用本地 Playwright；Playwright smoke 使用临时 App Bridge `127.0.0.1:18794` + Vite `127.0.0.1:5178`，预置完整 tool session 后故意拦截 `/api/events` 为 503，Desktop 仍从 `/messages` 渲染 3 条 message row（user prompt、tool output `persisted-message`、assistant `tool execution completed`），Stream 显示 `Events0 Messages3`，Vite overlay 0、page errors 0，仅忽略预期的 `/api/events` 503 resource log，截图 `/tmp/openagent-desktop-message-persistence-smoke.png`；临时服务已停止。残留：message UI 仍是基础 transcript，不是完整 tool card/part inspector；SSE reconnect/resume UX 仍需单独收紧；message pagination/before UI 还没做。
  - 2026-07-01: Desktop 文件/Git + checkpoint restore 可视闭环完成。App Bridge 已有 `/api/files` 与 `/api/git` 读路径，本轮补强 `/api/files` 文本判断为先看 metadata，避免读取超大文件；Desktop 新增 Files/Git inspector 面板，显示 workspace、entry count、当前焦点文件预览、Git branch/ahead/behind/change list；checkpoint restore 按钮增加 `data-checkpoint-id` 便于稳定渲染烟测；approval/undo/redo/restore/session trust refresh 后会同步刷新文件与 Git 上下文。验证：`cargo fmt --all` 通过；`cargo test -p openagent-http-runtime app_bridge_trust_boundary_routes_list_approve_diff_and_restore_checkpoint -- --nocapture` 通过；`npm --prefix desktop run build` 通过两次；内置 Browser 路径在导航/快照阶段 30s 超时并重置，按前端验证 fallback 使用本地 Playwright；Playwright smoke 使用临时 App Bridge `127.0.0.1:18793` + Vite `127.0.0.1:5177`，创建 pending write approval，从 Desktop 点击 Allow，制造 `beta` 版本，刷新后点击指定 checkpoint Restore，Files 面板从 `beta` 回到 `alpha`，Git 面板显示目标文件，Vite overlay 0、console/page errors 0，截图 `/tmp/openagent-desktop-files-git-restore-smoke.png`；临时服务已停止。残留：文件 API 仍是只读/轻量 file tree，不是完整编辑器；Git API 仍是 status-only，不含 stage/commit/diff 操作；Desktop 仍缺 PTY terminal、真实项目选择、MCP 面板和 Tauri 打包。
  - 2026-07-01: Desktop 信任边界最小产品面完成。App Bridge protocol manifest 扩展 `/api/approvals`、`/api/questions`、`/api/sessions/{id}/checkpoints` 和 checkpoint restore；新增 `GET /api/approvals`/`GET /api/questions` pending 队列，`POST /api/approvals/{request_id}`/`POST /api/questions/{request_id}/reply` Desktop-friendly global response route，保留原 turn-scoped route；新增 `GET /api/sessions/{id}/checkpoints` 和 `POST /api/sessions/{id}/checkpoints/{checkpoint_id}/restore`，restore 会写 `checkpoint/restored` App Bridge event。Desktop 右侧 inspector 新增 Trust、Diff、Checkpoints 面板，支持 Allow/Deny、Question Reply/Dismiss、Undo/Redo、Checkpoint Restore 按钮，并展示 diff preview/checkpoint list。验证：`cargo fmt --all` 通过；`cargo test -p openagent-http-runtime app_bridge_trust_boundary_routes_list_approve_diff_and_restore_checkpoint -- --nocapture` 通过，覆盖 pending approval 列出、global approve、diff、checkpoint list、restore；`npm --prefix desktop run build` 通过；临时 `/tmp` workspace App Bridge + Vite + Playwright smoke 显示 Trust 无 pending、Diff `+alpha`、Checkpoints 3 条和 3 个 restore 按钮，截图 `/tmp/openagent-trust-current.png`；临时 18792/5176 服务已停止。残留：UI smoke 没点击 restore 按钮，question 面板有 UI 和 API 但本轮未做 rendered question flow。
  - 2026-07-01: Desktop 最小垂直闭环第一步完成。新增 `desktop/` Tauri v2 + React/TypeScript/Vite shell，包含左侧项目/会话/Bridge 配置，中间 timeline + composer，右侧 protocol/provider/stream inspector；前端接入 App Bridge `GET /api/protocol`、`GET /api/models?check=true`、`GET/POST /api/sessions`、`POST /api/sessions/{id}/turns` 和 `/api/events` SSE；修复 React StrictMode 下事件去重副作用导致 timeline 不显示事件的问题；`.gitignore` 增加 `node_modules/`，避免桌面依赖污染提交。验证：`npm --prefix desktop run build` 通过；临时 Rust App Bridge + Vite smoke 通过，Playwright 预置 bridge URL 后完成打开 Desktop -> 新建 session -> 提交 prompt -> App Bridge 真实调用 Sub2API `gpt-5.4-mini` -> SSE/timeline 展示 7 个事件 -> completed 卡片显示 `desktop-smoke-3`；控制台错误 0、无 Vite overlay；截图 `/tmp/openagent-desktop-smoke.png` 已检查；临时 18791/5175 服务已停止。
  - 2026-07-01: App Bridge protocol contract 闭环推进。新增 `GET /api/protocol` machine-readable manifest，固定 `openagent.app_bridge` protocol v1、`openagent.app_event.v1` event schema、required event envelope fields、SSE resume/live query、endpoint map、event method list和 terminal methods；`append_app_events` 持久化事件时自动补 `schema_version` 与 `protocol_version`；`RemoteRuntimeClient` 新增 `protocol()`、`global_events_live()`、`turn_events_live()` 和带 `Accept: text/event-stream` 的 live SSE helper，避免 Desktop/TUI/CLI 各自手写订阅。验证：`cargo test -p openagent-http-runtime app_bridge_protocol_contract_and_client_live_subscription -- --nocapture` 通过；`cargo test -p openagent-http-runtime global_sse_live_tails_events_after_connection -- --nocapture` 通过；`cargo test -p openagent-http-runtime app_bridge_provider_health_uses_runtime_provider_config_without_leaking_key -- --nocapture` 通过；`cargo check -p openagent-http-runtime -p openagent-app-server-client` 通过；`cargo test -p openagent-app-server-client --lib -- --nocapture` 通过；`cargo fmt --all -- --check` 通过；本地 HTTP smoke `GET /api/protocol` 返回 `openagent.app_bridge`、protocol v1、`openagent.app_event.v1` 和 `/api/events` contract。
  - 2026-07-01: App Bridge provider diagnostics 闭环推进。`GET /api/models` 从静态假模型升级为 runtime provider 配置视图，`GET /api/models?check=true` 真实探测 OpenAI-compatible `/models`，返回 provider/base/model/wire_api 来源、key set/missing、model_count、configured_model_available、healthy 等安全字段；`RemoteRuntimeClient::provider_health()` 封装同一 endpoint；HTTP runtime 的实际 `provider_turn_result` 复用同一 resolver，支持 env、generic OPENAGENT_*、auth.json 和 session/payload 覆盖；普通 HTTP runtime 测试 helper 默认隔离本机真实 provider env/auth，避免测试误打公网。验证：`cargo test -p openagent-http-runtime app_bridge_provider_health_uses_runtime_provider_config_without_leaking_key -- --nocapture` 通过；`cargo test -p openagent-http-runtime remote_runtime_client_controls_model_agent_variant_and_thinking -- --nocapture` 通过；`cargo check -p openagent-http-runtime -p openagent-app-server-client` 通过；`cargo fmt --all -- --check` 通过；真实 Sub2API App Bridge smoke `GET /api/models?check=true` 返回 healthy true、`gpt-5.4-mini`、17 models、configured model available、key 仅显示 `set`。
  - 2026-07-01: Rust doctor/provider 配置闭环推进。新增统一 provider 配置解析，按 CLI flag -> env -> 显式 env file -> auth.json -> provider default 解析 api_key/base_url/model/wire_api；doctor 改为真实 GET `/models` 并输出 endpoint/model_count/configured_model_available；run 复用同一解析路径从 auth/env 获取 provider 配置；config show 只展示 key 状态和来源；本地私有 `.openagent/openagent.env` 默认模型对齐为 `gpt-5.4-mini`。验证：`cargo test -p openagent-cli binary_doctor_json -- --nocapture` 通过；`cargo test -p openagent-cli binary_run_uses_auth_file_provider_config_without_skip_doctor -- --nocapture` 通过；`cargo fmt --all -- --check` 通过；Sub2API `doctor --format json` 返回 healthy true、HTTP 200、17 models、`gpt-5.4-mini` listed；Sub2API `run --format json --model gpt-5.4-mini --max-steps 1 'Reply with exactly: pong'` 不加 `--skip-doctor` 成功返回 `pong`。
- next_recommended_slice: 做用户可见大闭环：Desktop MCP panel 可用化，或 approval dock 产品化；只在闭环完成后集中验证。
- blockers:
  - 无当前阻塞。
