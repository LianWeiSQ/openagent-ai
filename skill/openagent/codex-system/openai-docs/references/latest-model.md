# Latest model guide

This file is a curated helper. Every recommendation here must be verified against current OpenAI docs before it is repeated to a user.

## Current model map

| Model ID | Use for |
| --- | --- |
| `gpt-5.5` | Default text and coding model for local OpenAgent smoke and product workflows |
| `gpt-5.4` | Previous text and coding model for compatibility checks |
| `gpt-image-2` | Image generation and editing |
| `gpt-image-1.5` | Image generation and editing compatibility |

## Maintenance notes

- This file will drift unless it is periodically re-verified against current OpenAI docs.
- If this file conflicts with current docs, the docs win.
